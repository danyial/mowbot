---
phase: 00-gsd-brownfield-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md
  - .planning/STATE.md
autonomous: true
requirements: [META-01]
must_haves:
  truths:
    - "A human reader can find a single receipt file documenting when/how MowerBot was adopted as GSD-managed baseline."
    - "A tooling-readable git anchor (annotated tag gsd-baseline-v0) points at the adoption commit."
    - "All three ROADMAP §Phase 0 success criteria are verified against the actual repo state — not asserted."
    - "STATE.md reflects Phase 0 complete and Phase 1 as next focus."
    - "Uncommitted hardware/ WIP is explicitly called out as deferred, not silently ignored."
  artifacts:
    - path: ".planning/phases/00-gsd-brownfield-adoption/ADOPTION.md"
      provides: "Human-readable adoption receipt with date, baseline commit SHA, evidence table, tag name, and hardware/ dirty-tree disclosure."
      contains: "## Adoption Receipt"
    - path: ".planning/STATE.md"
      provides: "Updated phase position (Phase 0 complete → Phase 1 next) per D-09."
      contains: "Phase 1"
    - path: "git tag: gsd-baseline-v0"
      provides: "Annotated tag on the Phase 0 completion commit, referencing PROJECT.md and 'LD19 LiDAR Integration'."
  key_links:
    - from: "ADOPTION.md evidence table"
      to: ".planning/PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md, config.json, .planning/codebase/*.md"
      via: "file paths + git blob/commit SHAs"
      pattern: "\\.planning/(PROJECT|REQUIREMENTS|ROADMAP|STATE)\\.md"
    - from: "git tag gsd-baseline-v0"
      to: "Phase 0 completion commit (containing ADOPTION.md + STATE.md update)"
      via: "git tag -a annotation"
      pattern: "gsd-baseline-v0"
---

<objective>
Formally adopt the existing MowerBot repo as the GSD-managed brownfield baseline. Produce an adoption receipt file (ADOPTION.md), verify the three ROADMAP Phase 0 success criteria programmatically, update STATE.md to mark Phase 0 complete, commit, and place an annotated git tag `gsd-baseline-v0` as the canonical baseline anchor.

Purpose: Create a durable, forensics-ready marker separating "pre-GSD brownfield" from "GSD-managed" history. No product code, firmware, hardware, web, or docker changes. Per CONTEXT.md D-01, this is pure attestation.

Output: `.planning/phases/00-gsd-brownfield-adoption/ADOPTION.md`, updated `.planning/STATE.md`, one commit, annotated tag `gsd-baseline-v0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/config.json
@.planning/phases/00-gsd-brownfield-adoption/00-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 0 success criteria programmatically</name>
  <files>(read-only verification — no files modified)</files>
  <read_first>
    - .planning/ROADMAP.md (§"Phase 0: GSD Brownfield Adoption" success criteria)
    - .planning/PROJECT.md (to confirm Core Value / v1 scope / out-of-scope are identifiable from this file alone)
  </read_first>
  <action>
    Run the following verification commands from repo root and capture each result for use in Task 2's evidence table. Do NOT modify any file in this task — read-only.

    Per D-02 criterion 1 (core .planning/ files present, committed, reflect LD19 milestone):
    ```
    for f in .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/config.json; do
      test -f "$f" && echo "EXISTS: $f" || echo "MISSING: $f"
      git ls-files --error-unmatch "$f" >/dev/null 2>&1 && echo "COMMITTED: $f" || echo "UNTRACKED: $f"
      git log -n1 --pretty=format:'%H %s' -- "$f"
      echo
    done
    grep -q "LD19" .planning/PROJECT.md && echo "LD19-REF: PROJECT.md OK" || echo "LD19-REF: PROJECT.md MISSING"
    grep -q "LD19" .planning/ROADMAP.md && echo "LD19-REF: ROADMAP.md OK" || echo "LD19-REF: ROADMAP.md MISSING"
    grep -q "LD19" .planning/REQUIREMENTS.md && echo "LD19-REF: REQUIREMENTS.md OK" || echo "LD19-REF: REQUIREMENTS.md MISSING"
    ```

    Per D-02 criterion 2 (codebase artifacts present):
    ```
    for f in ARCHITECTURE.md STACK.md STRUCTURE.md CONCERNS.md CONVENTIONS.md INTEGRATIONS.md TESTING.md; do
      test -f ".planning/codebase/$f" && echo "EXISTS: .planning/codebase/$f" || echo "MISSING: .planning/codebase/$f"
      git ls-files --error-unmatch ".planning/codebase/$f" >/dev/null 2>&1 && echo "COMMITTED: .planning/codebase/$f" || echo "UNTRACKED: .planning/codebase/$f"
      git log -n1 --pretty=format:'%H' -- ".planning/codebase/$f"
      echo
    done
    ```

    Per D-02 criterion 3 (PROJECT.md alone conveys core value, v1 scope, out-of-scope):
    ```
    grep -nE "^(## |### )(Core Value|Scope|Out of Scope|Out-of-Scope|v1|V1)" .planning/PROJECT.md
    grep -nE "core value|Core Value" .planning/PROJECT.md
    ```
    Manually confirm PROJECT.md contains identifiable sections for (a) core value = LD19 → /scan → web map, (b) v1 scope, (c) explicit out-of-scope. Record the exact heading text + line numbers for use in the evidence table.

    Capture the baseline commit SHA (the current HEAD before this phase's commit):
    ```
    git rev-parse HEAD
    git rev-parse --short HEAD
    ```

    Capture the dirty hardware/ tree state (per D-06, D-07 — do NOT stage or commit):
    ```
    git status --porcelain -- hardware/ MowerBot-MotorController-erc.rpt docs/pcb-motor-controller.md
    ```

    Save all outputs to scratch (mentally or as a note) — Task 2 writes them into ADOPTION.md.
  </action>
  <verify>
    <automated>bash -c 'set -e; for f in .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/config.json .planning/codebase/ARCHITECTURE.md .planning/codebase/STACK.md .planning/codebase/STRUCTURE.md .planning/codebase/CONCERNS.md .planning/codebase/CONVENTIONS.md .planning/codebase/INTEGRATIONS.md .planning/codebase/TESTING.md; do test -f "$f" || { echo "MISSING $f"; exit 1; }; git ls-files --error-unmatch "$f" >/dev/null 2>&1 || { echo "UNTRACKED $f"; exit 1; }; done; grep -q "LD19" .planning/PROJECT.md && grep -q "LD19" .planning/ROADMAP.md && grep -q "LD19" .planning/REQUIREMENTS.md; echo "ALL PHASE 0 CRITERIA EVIDENCE PRESENT"'</automated>
  </verify>
  <done>
    All 12 required files (5 core + 7 codebase) are confirmed present AND committed (tracked by git). All three core .planning/ docs contain the string "LD19". PROJECT.md sections for core value, v1 scope, and out-of-scope are identified with exact heading text and line numbers. Baseline HEAD SHA and per-file last-commit SHAs are captured for Task 2. Hardware/ dirty-tree file list is captured for Task 2's disclosure section. The verification bash one-liner above exits 0 and prints "ALL PHASE 0 CRITERIA EVIDENCE PRESENT".
  </done>
  <acceptance_criteria>
    - `test -f .planning/PROJECT.md` → exit 0
    - `test -f .planning/REQUIREMENTS.md` → exit 0
    - `test -f .planning/ROADMAP.md` → exit 0
    - `test -f .planning/STATE.md` → exit 0
    - `test -f .planning/config.json` → exit 0
    - `test -f .planning/codebase/ARCHITECTURE.md` → exit 0 (and same for STACK, STRUCTURE, CONCERNS, CONVENTIONS, INTEGRATIONS, TESTING)
    - `git ls-files --error-unmatch <each-path>` → exit 0 for all 12 paths
    - `grep -q "LD19" .planning/PROJECT.md` → exit 0
    - `grep -q "LD19" .planning/ROADMAP.md` → exit 0
    - `grep -q "LD19" .planning/REQUIREMENTS.md` → exit 0
    - `grep -nE "^(## |### )" .planning/PROJECT.md` returns at least one heading matching "Core Value" (case-insensitive) and one matching "Out of Scope" or "Out-of-Scope" (case-insensitive)
    - `git rev-parse HEAD` captured (exact SHA recorded for Task 2)
    - `git status --porcelain -- hardware/` captured for Task 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create ADOPTION.md receipt</name>
  <files>.planning/phases/00-gsd-brownfield-adoption/ADOPTION.md</files>
  <read_first>
    - .planning/phases/00-gsd-brownfield-adoption/00-CONTEXT.md (D-03, D-04, D-07 for required content)
    - Evidence captured in Task 1 (baseline SHA, per-file SHAs, hardware/ dirty list)
  </read_first>
  <action>
    Create `.planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` with EXACTLY these section headings in this order (per D-03, D-05, D-07; format is planner discretion but headings below are REQUIRED verbatim):

    1. `# MowerBot GSD Adoption Receipt` (H1 title)
    2. `## Adoption Receipt` (H2 — summary block: Date, Milestone, Baseline Commit SHA, Tag Name)
    3. `## Success Criteria Evidence` (H2 — table covering the three ROADMAP §Phase 0 success criteria)
    4. `## Known State at Adoption` (H2 — hardware/ dirty-tree disclosure per D-07)
    5. `## Baseline Anchor` (H2 — explicit statement that ADOPTION.md + tag `gsd-baseline-v0` together constitute the canonical adoption point per D-05)

    Required fields inside `## Adoption Receipt`:
    - **Date:** 2026-04-14
    - **Milestone:** LD19 LiDAR Integration
    - **Baseline Commit SHA:** `<full 40-char HEAD SHA captured in Task 1>` (this is the pre-ADOPTION HEAD)
    - **Tag (to be created on this phase's commit):** `gsd-baseline-v0`
    - **Project reference:** [.planning/PROJECT.md](../../PROJECT.md)

    Required evidence table under `## Success Criteria Evidence` — exactly these columns:
    `| Criterion | Evidence (path) | Last Commit SHA | Status |`
    Rows MUST include one row per file for criterion 1 (PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md, config.json), one row per file for criterion 2 (the 7 codebase/*.md artifacts), and at least one row for criterion 3 citing specific PROJECT.md heading(s) and line number(s) that demonstrate core value, v1 scope, and out-of-scope are identifiable from PROJECT.md alone. Status column = `✓ present` or `✓ verified`.

    Required content inside `## Known State at Adoption` (verbatim language required — this satisfies D-07):
    - A paragraph stating that uncommitted files under `hardware/` (PCB v2.0 `.kicad_*` edits, `.step` models, DRC reports, `production/`, `backups/`) plus `MowerBot-MotorController-erc.rpt` and modifications to `docs/pcb-motor-controller.md` are DEFERRED to Phase 1 per CONTEXT.md D-06.
    - A bulleted list reproducing the exact `git status --porcelain` output captured in Task 1 for the hardware/ WIP.
    - An explicit sentence: "This is known state, not a regression. Phase 1 (Hardware & UART Routing) inherits and commits these."

    Required content inside `## Baseline Anchor`:
    - State that `ADOPTION.md` + annotated git tag `gsd-baseline-v0` together are the canonical adoption anchor (per D-05).
    - Note that `/gsd-forensics` and pre/post-adoption diffs should reference the tag.
    - Note that the tag is created LOCALLY only (not pushed) per CONTEXT.md deferred-ideas section.

    Keep the whole file to roughly one screen. Machine-greppable SHAs (full 40-char) in the evidence table. Do NOT edit any other file in this task.
  </action>
  <verify>
    <automated>bash -c 'set -e; F=.planning/phases/00-gsd-brownfield-adoption/ADOPTION.md; test -f "$F"; grep -q "^# MowerBot GSD Adoption Receipt$" "$F"; grep -q "^## Adoption Receipt$" "$F"; grep -q "^## Success Criteria Evidence$" "$F"; grep -q "^## Known State at Adoption$" "$F"; grep -q "^## Baseline Anchor$" "$F"; grep -q "gsd-baseline-v0" "$F"; grep -q "LD19 LiDAR Integration" "$F"; grep -q "2026-04-14" "$F"; grep -qE "[0-9a-f]{40}" "$F"; grep -q "hardware/" "$F"; grep -q "Phase 1" "$F"; echo "ADOPTION.md OK"'</automated>
  </verify>
  <done>
    `.planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` exists with all 5 required headings verbatim, contains the baseline commit SHA as a full 40-char hex, mentions `gsd-baseline-v0`, mentions `LD19 LiDAR Integration`, discloses the dirty `hardware/` tree and defers it to Phase 1, and is ≤ ~120 lines. Verification one-liner exits 0 and prints `ADOPTION.md OK`.
  </done>
  <acceptance_criteria>
    - `grep -q "^# MowerBot GSD Adoption Receipt$" .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` → exit 0
    - `grep -q "^## Adoption Receipt$" .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` → exit 0
    - `grep -q "^## Success Criteria Evidence$" .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` → exit 0
    - `grep -q "^## Known State at Adoption$" .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` → exit 0
    - `grep -q "^## Baseline Anchor$" .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` → exit 0
    - `grep -q "gsd-baseline-v0" .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` → exit 0
    - `grep -q "LD19 LiDAR Integration" .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` → exit 0
    - `grep -qE "[0-9a-f]{40}" .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` → exit 0 (full baseline SHA present)
    - `grep -c "| .*\\.md .*| [0-9a-f]\\{40\\}" .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` ≥ 12 (evidence rows for 5 core + 7 codebase files)
    - `grep -q "Phase 1" .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` → exit 0 (hardware/ deferral called out)
    - `git status --porcelain -- hardware/` is unchanged from Task 1 (we did NOT stage hardware/)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md — Phase 0 complete, Phase 1 next</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current content — we edit targeted lines only)
    - .planning/phases/00-gsd-brownfield-adoption/00-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    Apply these exact edits to `.planning/STATE.md`. All other content in the file MUST remain unchanged. Per D-09, this update IS part of Phase 0's completion work.

    1. Line 3 — replace:
       `**Last updated:** 2026-04-14 (roadmap creation)`
       with:
       `**Last updated:** 2026-04-14 (Phase 0 complete — baseline tagged)`

    2. Line 9 — replace:
       `- **Current focus:** Roadmap created; awaiting Phase 0 plan generation.`
       with:
       `- **Current focus:** Phase 0 complete; baseline adopted as `gsd-baseline-v0`; Phase 1 (Hardware & UART Routing) is next.`

    3. Lines 13–16 — replace the four Current Position lines verbatim:
       ```
       - **Phase:** 0 — GSD Brownfield Adoption (not started)
       - **Plan:** —
       - **Status:** Roadmap approved, no plans generated yet
       - **Progress:** `[░░░░░░░░░░] 0/4 phases complete`
       ```
       with:
       ```
       - **Phase:** 1 — Hardware & UART Routing (not started)
       - **Plan:** —
       - **Status:** Phase 0 complete; baseline committed and tagged `gsd-baseline-v0`; awaiting Phase 1 plan generation
       - **Progress:** `[██░░░░░░░░] 1/4 phases complete`
       ```

    4. Lines 18–22 — replace the ASCII dependency block:
       ```
       Phase 0 ──► Phase 1 ──► Phase 2 ──► Phase 3 (Core Value)
        META        HW/UART     Driver      Web viz
        [ now ]
       ```
       with:
       ```
       Phase 0 ──► Phase 1 ──► Phase 2 ──► Phase 3 (Core Value)
        META        HW/UART     Driver      Web viz
        [done]      [ now ]
       ```

    5. In the `### Active todos` section (around line 55): replace the line
       `- [ ] Generate Phase 0 plans via `/gsd-plan-phase 0``
       with:
       `- [x] Generate Phase 0 plans via `/gsd-plan-phase 0``
       And on the following line keep:
       `- [ ] After Phase 0: generate Phase 1 plans (UART routing is the gating hardware decision)`
       (unchanged).

    6. In the `### Recent events` section (after the existing 2026-04-14 lines), APPEND this new bullet as the last entry in the list:
       `- 2026-04-14 — Phase 0 complete: `.planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` written; annotated tag `gsd-baseline-v0` placed on adoption commit; brownfield baseline formally adopted under GSD.`

    7. Final line of file — replace:
       `*State initialized: 2026-04-14 after roadmap creation*`
       with:
       `*State initialized: 2026-04-14 after roadmap creation*`
       `*Last transition: 2026-04-14 — Phase 0 → Phase 1*`

    Do NOT modify any other sections (Performance Metrics table, Key decisions, Research anchors, Blockers, Resumption context). Do NOT edit any file besides `.planning/STATE.md`.
  </action>
  <verify>
    <automated>bash -c 'set -e; F=.planning/STATE.md; grep -q "^**Last updated:** 2026-04-14 (Phase 0 complete" "$F" || grep -q "Phase 0 complete — baseline tagged" "$F"; grep -q "Phase 1 — Hardware & UART Routing (not started)" "$F"; grep -q "gsd-baseline-v0" "$F"; grep -q "1/4 phases complete" "$F"; grep -q "\\[done\\]" "$F"; grep -q "\\[x\\] Generate Phase 0 plans" "$F"; grep -q "Phase 0 → Phase 1" "$F"; echo "STATE.md OK"'</automated>
  </verify>
  <done>
    `.planning/STATE.md` reflects: Phase 0 complete, Phase 1 now the active phase, progress 1/4, the `[done]` marker on the Phase 0 ASCII block, the Phase 0 todo checked off, a new Recent events entry citing ADOPTION.md and `gsd-baseline-v0`, and a "Last transition" footer. No other sections modified. Verification one-liner exits 0 and prints `STATE.md OK`.
  </done>
  <acceptance_criteria>
    - `grep -q "Phase 1 — Hardware & UART Routing (not started)" .planning/STATE.md` → exit 0
    - `grep -q "gsd-baseline-v0" .planning/STATE.md` → exit 0
    - `grep -q "1/4 phases complete" .planning/STATE.md` → exit 0
    - `grep -q "\[done\]" .planning/STATE.md` → exit 0
    - `grep -q "\[x\] Generate Phase 0 plans" .planning/STATE.md` → exit 0
    - `grep -q "Phase 0 → Phase 1" .planning/STATE.md` → exit 0
    - `grep -c "Phase 0 complete" .planning/STATE.md` ≥ 1
    - `git diff --name-only -- .planning/` shows ONLY `.planning/STATE.md` (and new ADOPTION.md) — no other .planning/ files changed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Commit Phase 0 completion and create annotated tag `gsd-baseline-v0`</name>
  <files>(git operation — no new file edits; commits ADOPTION.md + STATE.md + CONTEXT/PLAN per commit_docs=true)</files>
  <read_first>
    - .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md (confirm it exists and is complete)
    - .planning/STATE.md (confirm it reflects Phase 0 complete)
    - .planning/config.json (confirm `git.branching_strategy: "none"` — commit lands on main)
  </read_first>
  <action>
    Sanity-precheck BEFORE staging:
    ```
    git status --porcelain
    ```
    Confirm the only expected changes to stage are:
    - Modified: `.planning/STATE.md`
    - New:      `.planning/phases/00-gsd-brownfield-adoption/ADOPTION.md`
    - Plus any CONTEXT/PLAN files in `.planning/phases/00-gsd-brownfield-adoption/` that `commit_docs: true` expects to land with this phase (00-CONTEXT.md, 00-01-PLAN.md).

    Explicitly DO NOT stage:
    - Anything under `hardware/`
    - `MowerBot-MotorController-erc.rpt`
    - Modifications to `docs/pcb-motor-controller.md`
    - Any product / firmware / web / docker file

    Stage with explicit paths (NOT `git add -A` / `git add .`):
    ```
    git add .planning/STATE.md
    git add .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md
    git add .planning/phases/00-gsd-brownfield-adoption/00-CONTEXT.md
    git add .planning/phases/00-gsd-brownfield-adoption/00-01-PLAN.md
    ```

    Verify staging is clean of forbidden paths:
    ```
    git diff --cached --name-only | grep -vE '^\.planning/' && { echo "FORBIDDEN PATH STAGED"; exit 1; } || echo "staging clean"
    git diff --cached --name-only | grep -E '^hardware/' && { echo "HARDWARE STAGED"; exit 1; } || echo "no hardware staged"
    ```

    Create the commit using a HEREDOC message:
    ```
    git commit -m "$(cat <<'EOF'
    docs(phase-0): adopt MowerBot as GSD brownfield baseline

    Phase 0 (GSD Brownfield Adoption) complete. Writes the adoption receipt
    (.planning/phases/00-gsd-brownfield-adoption/ADOPTION.md) and updates
    STATE.md to reflect Phase 0 → Phase 1 transition. No product, firmware,
    web, docker, or hardware code touched — pure attestation per CONTEXT.md
    D-01. Uncommitted hardware/ WIP is explicitly deferred to Phase 1
    (CONTEXT.md D-06, D-07).

    Anchor: annotated tag `gsd-baseline-v0` on this commit.
    Milestone: LD19 LiDAR Integration.
    Requirement: META-01.

    Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Capture the resulting commit SHA:
    ```
    COMMIT_SHA=$(git rev-parse HEAD)
    echo "Phase 0 completion commit: $COMMIT_SHA"
    ```

    Create the annotated tag on this commit — EXACT tag name `gsd-baseline-v0`, EXACT `-a` flag for annotation, message template below (per D-04):
    ```
    git tag -a gsd-baseline-v0 -m "$(cat <<'EOF'
    MowerBot GSD Brownfield Baseline (v0)

    Canonical adoption anchor for MowerBot under the GSD workflow.
    Milestone: LD19 LiDAR Integration.

    See .planning/PROJECT.md for core value, v1 scope, and out-of-scope.
    See .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md for the
    full adoption receipt and success-criteria evidence.

    This tag + ADOPTION.md together are the canonical adoption point.
    Pre-adoption vs GSD-managed forensics should reference this tag.
    EOF
    )"
    ```

    Do NOT push the tag (per CONTEXT.md deferred — user controls remote sync).

    Final verification:
    ```
    git tag -l gsd-baseline-v0
    git rev-parse gsd-baseline-v0
    git for-each-ref refs/tags/gsd-baseline-v0 --format='%(objecttype) %(taggername) %(contents:subject)'
    git log -1 --pretty=format:'%H %s'
    git status --porcelain -- hardware/ | head -5   # should STILL show hardware/ WIP (untouched)
    ```
  </action>
  <verify>
    <automated>bash -c 'set -e; git tag -l gsd-baseline-v0 | grep -q "^gsd-baseline-v0$"; git cat-file -t "$(git rev-parse gsd-baseline-v0)" | grep -q "^tag$"; git for-each-ref refs/tags/gsd-baseline-v0 --format="%(contents)" | grep -q "LD19 LiDAR Integration"; git for-each-ref refs/tags/gsd-baseline-v0 --format="%(contents)" | grep -q "PROJECT.md"; git log -1 --pretty=%B | grep -q "Phase 0"; git log -1 --name-only --pretty=format: | grep -qv "^hardware/"; echo "TAG AND COMMIT OK"'</automated>
  </verify>
  <done>
    One new commit on main containing ONLY `.planning/STATE.md` + `.planning/phases/00-gsd-brownfield-adoption/{ADOPTION,00-CONTEXT,00-01-PLAN}.md`; annotated tag `gsd-baseline-v0` exists locally, points at that commit, tag object type is `tag` (not `commit` — i.e. annotated), and the annotation message references both `PROJECT.md` and `LD19 LiDAR Integration`. Hardware/ WIP remains untouched in working tree. Tag NOT pushed. Verification one-liner exits 0 and prints `TAG AND COMMIT OK`.
  </done>
  <acceptance_criteria>
    - `git tag -l gsd-baseline-v0` → prints `gsd-baseline-v0`
    - `git cat-file -t $(git rev-parse gsd-baseline-v0)` → prints `tag` (annotated, not lightweight)
    - `git for-each-ref refs/tags/gsd-baseline-v0 --format='%(contents)'` contains literal "LD19 LiDAR Integration"
    - `git for-each-ref refs/tags/gsd-baseline-v0 --format='%(contents)'` contains literal "PROJECT.md"
    - `git rev-parse gsd-baseline-v0^{commit}` equals `git rev-parse HEAD` (tag points at Phase 0 completion commit)
    - `git log -1 --name-only --pretty=format:` lists ONLY paths under `.planning/` (no hardware/, no firmware/, no web/, no docker/, no docs/)
    - `git log -1 --pretty=%B` contains "Phase 0" and "META-01" and "gsd-baseline-v0"
    - `git status --porcelain -- hardware/` still shows WIP files (they were NOT staged/committed)
    - `git ls-remote --tags origin gsd-baseline-v0` is empty OR the command is simply NOT run (tag intentionally not pushed)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Phase-level verification (run after all 4 tasks):

1. All 12 required baseline files still present and committed:
   ```
   for f in .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/config.json .planning/codebase/ARCHITECTURE.md .planning/codebase/STACK.md .planning/codebase/STRUCTURE.md .planning/codebase/CONCERNS.md .planning/codebase/CONVENTIONS.md .planning/codebase/INTEGRATIONS.md .planning/codebase/TESTING.md; do git ls-files --error-unmatch "$f" >/dev/null || { echo "MISSING OR UNTRACKED: $f"; exit 1; }; done
   ```

2. ADOPTION.md receipt exists with required structure:
   `grep -c "^## " .planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` ≥ 4

3. STATE.md reflects Phase 0 → Phase 1 transition:
   `grep -q "Phase 1 — Hardware & UART Routing" .planning/STATE.md && grep -q "gsd-baseline-v0" .planning/STATE.md`

4. Annotated tag placed locally:
   `git cat-file -t $(git rev-parse gsd-baseline-v0)` returns `tag`

5. No forbidden paths touched:
   `git log gsd-baseline-v0^..gsd-baseline-v0 --name-only --pretty=format: | grep -vE '^\.planning/|^$'` returns empty

6. Hardware/ WIP preserved untouched:
   `git status --porcelain -- hardware/` still reports the same files as at start of phase
</verification>

<success_criteria>
Phase 0 is complete when ALL of the following are TRUE:

- [ ] `.planning/phases/00-gsd-brownfield-adoption/ADOPTION.md` exists with all 5 required H1/H2 headings, full baseline SHA, evidence table covering 12 files, hardware/ disclosure, and baseline-anchor statement.
- [ ] `.planning/STATE.md` shows Phase 0 complete, Phase 1 as current focus, progress 1/4, and a new Recent events entry referencing ADOPTION.md + `gsd-baseline-v0`.
- [ ] Annotated git tag `gsd-baseline-v0` exists locally, points at the Phase 0 completion commit, and its message references both `PROJECT.md` and `LD19 LiDAR Integration`.
- [ ] Exactly one new commit on main; it touches ONLY files under `.planning/`.
- [ ] No edits to product code, firmware, web, docker, hardware/, or to any .planning/ file other than STATE.md (plus new ADOPTION.md and phase CONTEXT/PLAN).
- [ ] Uncommitted `hardware/` WIP is unchanged in the working tree.
- [ ] Tag NOT pushed to remote.
- [ ] All three ROADMAP §Phase 0 success criteria verified and cited in ADOPTION.md evidence table.
- [ ] META-01 requirement satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/00-gsd-brownfield-adoption/00-01-SUMMARY.md` summarizing:
- Files created (ADOPTION.md) and modified (STATE.md)
- Commit SHA of Phase 0 completion commit
- Tag created (`gsd-baseline-v0`, annotated, local only)
- Confirmation that all three ROADMAP Phase 0 success criteria were verified with evidence
- Confirmation that hardware/ WIP remains deferred to Phase 1
</output>
