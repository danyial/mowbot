---
phase: 2
slug: lidar-driver-scan-publication
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-14
---

# Phase 2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Shell + `ros2` CLI tooling (no unit-test framework in repo) |
| **Config file** | none — aggregator script `validate.sh` is Wave 0 deliverable (Task 16) |
| **Quick run command** | `bash .planning/phases/02-lidar-driver-scan-publication/validate.sh` |
| **Full suite command** | `bash .planning/phases/02-lidar-driver-scan-publication/validate.sh --full` |
| **Estimated runtime** | ~90 s (quick) / ~4 min (full — includes §F regression matrix) |

---

## Sampling Rate

- **After every task commit:** Run the per-task `<verify><automated>` command from `02-01-PLAN.md`.
- **After Wave 1 (Commit A):** Run the full 12-row `§F Commit A Regression Matrix` (Task 4).
- **After Wave 2 (Commit B):** Run `validate.sh` (quick); before `/gsd-verify-work` run `validate.sh --full`.
- **Before `/gsd-verify-work`:** Full suite must be green (exit 0).
- **Max feedback latency:** ~90 seconds for quick run.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 02-01-01 | 01 | 1 | DRV (setup) | — | Baseline captured before mutation | integration | `test -s /tmp/compose-pre-retrofit.yaml && grep -q 'micro-ros-agent' /tmp/compose-pre-retrofit.yaml` | ✅ | ⬜ pending |
| 02-01-02 | 01 | 1 | DRV (setup) | T-02-01 | Anchor edited idempotently (exactly 1 occurrence each) | static | `grep -c 'ipc: host' docker-compose.yml \| grep -q '^1$' && grep -c 'pid: host' docker-compose.yml \| grep -q '^1$'` | ✅ | ⬜ pending |
| 02-01-03 | 01 | 1 | DRV (regression) | T-02-01 | All 7 pre-existing services healthy with ipc/pid=host | integration | `grep -cE 'mower-(micro-ros\|gnss\|imu\|nav\|ntrip\|rosbridge\|web)' /tmp/compose-ps-post-retrofit.txt \| grep -qE '^[7-9]$\|^1[0-9]$' && ! grep -qE 'Exit\|Restarting' /tmp/compose-ps-post-retrofit.txt` | ✅ | ⬜ pending |
| 02-01-04 | 01 | 1 | DRV (regression matrix §F) | T-02-01, T-02-06 | No pre-existing service regressed; topic set unchanged; stack survives reboot | integration | `for r in $(seq 1 12); do test -s /tmp/regressionA-$r.log \|\| exit 1; done; ! grep -l '^FAIL' /tmp/regressionA-*.log` | ✅ (generated by task) | ⬜ pending |
| 02-01-05 | 01 | 1 | DRV (commit) | — | Anchor retrofit isolated as a reversible commit | static | `git log -1 --format=%s \| grep -q 'retrofit ipc:host + pid:host'` | ✅ | ⬜ pending |
| 02-01-06 | 01 | 2 | DRV-01, DRV-05 | T-02-02, T-02-03 | Pinned SHA + build-time QoS assertion | static | `grep -q 'ARG LDLIDAR_SHA=bf668a89baf722a787dadc442860dcbf33a82f5a' docker/lidar/Dockerfile && grep -q 'rclcpp::SensorDataQoS()' docker/lidar/Dockerfile` | ✅ | ⬜ pending |
| 02-01-07 | 01 | 2 | DRV-03, DRV-04 | — | Launch params match MowerBot contract (frame, port, crop surface) | static | `python3 -c "import ast; ast.parse(open('docker/lidar/launch/lidar.launch.py').read())" && grep -q "'frame_id': 'laser_frame'" docker/lidar/launch/lidar.launch.py` | ✅ | ⬜ pending |
| 02-01-08 | 01 | 2 | DRV-01, DRV-05 | — | Compose service wired; pinned image tag | static | `grep -q 'container_name: mower-lidar' docker-compose.yml && grep -q 'ghcr.io/danyial/mowbot/lidar:bf668a8' docker-compose.yml` | ✅ | ⬜ pending |
| 02-01-09 | 01 | 2 | DRV-01 | T-02-03 | Image builds without QoS-patch silent no-op; container reaches running | integration | `grep -q 'running' /tmp/lidar-status.txt` | ✅ | ⬜ pending |
| 02-01-10 | 01 | 2 | DRV-01 | — | host/host/host + /dev/ttyLIDAR + pinned tag runtime-verified | integration | `grep -q 'host host host' /tmp/drv01-namespaces.txt && grep -q '/dev/ttyLIDAR=/dev/ttyLIDAR' /tmp/drv01-devices.txt && ! grep -q ':latest' /tmp/drv01-image.txt` | ✅ | ⬜ pending |
| 02-01-11 | 01 | 2 | DRV-02 | T-02-05 | /scan 10.0±0.1 Hz numeric band; BEST_EFFORT + KEEP_LAST(5) | integration | `awk -v r="$(cat /tmp/drv02-rate.txt)" 'BEGIN{exit !(r>=9.9 && r<=10.1)}' && grep -qE 'Reliability:[[:space:]]+BEST_EFFORT' /tmp/drv02-qos.txt && grep -qE 'KEEP_LAST \(5\)\|KEEP_LAST.*5' /tmp/drv02-qos.txt` | ✅ | ⬜ pending |
| 02-01-12 | 01 | 2 | DRV-03 | — | TF resolves; /scan frame_id=laser_frame | integration | `grep -qE 'At time\|Translation:' /tmp/drv03-tf.txt && ! grep -q 'Could not transform' /tmp/drv03-tf.txt && grep -q 'frame_id: laser_frame' /tmp/drv03-frame.txt` | ✅ | ⬜ pending |
| 02-01-13 | 01 | 2 | DRV-04 | — | angle_crop declarative surface present + scan message valid | static + integration | `grep -q 'angle_crop_min' /tmp/drv04-cropsurface.txt && grep -q 'enable_angle_crop_func' /tmp/drv04-cropsurface.txt && test -s /tmp/drv04-scan.txt` | ✅ | ⬜ pending |
| 02-01-14 | 01 | 2 | DRV-05 | — | Pinned tag + idempotent clean-start | integration | `grep -q 'ghcr.io/danyial/mowbot/lidar:bf668a8' /tmp/drv05-tag.txt && ! grep -q ':latest' /tmp/drv05-tag.txt && grep -q 'running RestartCount=0' /tmp/drv05-start.txt` | ✅ | ⬜ pending |
| 02-01-15 | 01 | 2 | DRV-03, DRV-04 (docs) | — | Mount-measurement doc covers base_link origin + crop procedure | static | `test -s docs/lidar-mount.md && grep -q 'base_link' docs/lidar-mount.md && grep -q 'enable_angle_crop_func' docs/lidar-mount.md` | ✅ | ⬜ pending |
| 02-01-16 | 01 | 2 | DRV-01..DRV-05 | — | Aggregator script syntactically valid + covers all DRVs | static | `test -x .planning/phases/02-lidar-driver-scan-publication/validate.sh && bash -n .planning/phases/02-lidar-driver-scan-publication/validate.sh` | ✅ | ⬜ pending |
| 02-01-17 | 01 | 2 | DRV-01..DRV-05 (commit) | — | Two-commit sequence (A then B) preserved on main | static | `git log -2 --format=%s \| head -1 \| grep -q 'feat(lidar)' && git log -2 --format=%s \| tail -1 \| grep -q 'retrofit ipc:host'` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `.planning/phases/02-lidar-driver-scan-publication/validate.sh` — DRV-01..DRV-05 aggregator + `--full` §F regression re-runner (created in Task 16)
- [ ] `/tmp/topics-pre-retrofit.txt` — baseline topic list (created in Task 1; consumed by Task 4 row 11)
- [ ] `/tmp/compose-pre-retrofit.yaml` — baseline rendered compose (created in Task 1)

*No unit-test framework install required — all validation is shell + `ros2` CLI, which are already available inside `mower-nav` and `mower-gnss` (RESEARCH Assumption A6).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Asymmetric obstacle on robot's left shows low-range at +π/2 | DRV-04 (hemisphere) | Requires a human to place a physical object 1 m to robot's left (+Y); no automation path on a stationary bench | Place cardboard wall ~1 m off robot's left side; run Task 13 hemisphere python inline; confirm `min_range_angle_rad` in (+0.7, +2.4) rad |
| Self-hit absence (visual no-spurious-near-field rings) | DRV-04 (deferred crop tuning) | Visual inspection of angular bin consistency over time — no programmatic oracle on a zero-placeholder TF | Stationary indoor test; `ros2 topic echo /scan --once` over 30 s; note angles with consistently <0.3 m returns in `docs/lidar-mount.md` for future crop config (NOT a Phase 2 pass gate per D-08) |
| TF measured values committed | DRV-03 (post-phase) | Requires physical mount + ruler/protractor measurement | Follow `docs/lidar-mount.md` procedure; Phase 2 ships zero placeholders per D-05/D-07 — actual measurement is post-phase |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (verified — all 17 tasks have `<automated>`)
- [ ] Wave 0 covers all MISSING references (validate.sh, baseline capture files)
- [ ] No watch-mode flags
- [ ] Feedback latency < 90 s (quick run)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
