---
id: 04-01
phase: 04-live-mapping-slam-toolbox
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [MAP-01, MAP-02, MAP-03, MAP-05]
files_modified:
  - docker/slam/Dockerfile
  - config/slam_toolbox_params.yaml
  - docker-compose.yml
  - .planning/REQUIREMENTS.md

must_haves:
  truths:
    - "docker compose up slam brings up mower-slam container, healthy, inheriting ros-common (network_mode/ipc/pid: host + CycloneDDS)"
    - "ros2 topic hz /map reports >= 0.5 Hz for 60 s under indoor /scan"
    - "ros2 topic echo /map --once prints a populated nav_msgs/OccupancyGrid"
    - "ros2 run tf2_tools view_frames produces a tree with map -> odom -> base_link -> laser_frame and map -> odom is published by slam_toolbox (not static)"
    - "With the robot physically stationary for 60 s indoors, the published OccupancyGrid does NOT drift (scan matcher does not fire below thresholds; reference wall cell stays within ±1 cell)"
    - "REQUIREMENTS.md contains MAP-01..MAP-05 with coverage table and traceability rows updated, committed"
  artifacts:
    - path: "docker/slam/Dockerfile"
      provides: "ros:humble-ros-base + ros-humble-slam-toolbox + CycloneDDS, CMD launches online_async with params file and use_sim_time:=false"
      contains: "ros-humble-slam-toolbox"
    - path: "config/slam_toolbox_params.yaml"
      provides: "Project-local slam_toolbox params overriding upstream defaults (base_link, do_loop_closing: false, transform_timeout: 1.0, map_update_interval: 2.0, max_laser_range: 12.0, use_map_saver: false, minimum_travel_{distance,heading}: 0.1)"
      contains: "base_frame: base_link"
    - path: "docker-compose.yml"
      provides: "New slam service inheriting *ros-common, pinned image tag, depends_on lidar+nav+micro-ros-agent, command uses online_async_launch.py + slam_params_file + use_sim_time:=false"
      contains: "slam:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MAP-01..MAP-05 in v1 requirements block + traceability rows mapping to Phase 4"
      contains: "MAP-01"
  key_links:
    - from: "docker-compose.yml slam service"
      to: "config/slam_toolbox_params.yaml"
      via: "volumes: ./config:/config:ro (inherited from *ros-common) + slam_params_file:=/config/slam_toolbox_params.yaml in command"
      pattern: "slam_params_file:=/config/slam_toolbox_params.yaml"
    - from: "slam_toolbox node"
      to: "EKF odom -> base_link TF (published by mower-nav)"
      via: "TF lookup at each scan timestamp; transform_timeout: 1.0 tolerates EKF jitter"
      pattern: "base_frame: base_link"
    - from: "slam_toolbox node"
      to: "LD19 /scan (10 Hz, BEST_EFFORT)"
      via: "default slam_toolbox scan subscriber QoS (sensor-data compatible)"
      pattern: "scan_topic: /scan"
---

<objective>
Stand up the `slam` ROS2 service: a pinned Humble slam_toolbox container running `online_async_launch.py` with a project-local params file that overrides the dangerous upstream defaults (base_footprint, use_sim_time true, loop closure on, 0.5 m travel threshold), wired into the existing docker-compose stack by inheriting the `*ros-common` anchor. After this plan deploys to the Pi, `/map` publishes at ≥ 0.5 Hz with a clean `map -> odom -> base_link -> laser_frame` TF tree, verified by SSH into `mower-slam`. This is the ROS-side half of Phase 4; Plan 04-02 consumes `/map` in the browser.

Purpose: Phase 4 SC #1 (container up + /map publishing), #2 (TF tree correct with map->odom from slam_toolbox), #4 (stationary stability — no drift for 60 s). Also lands the MAP-01..MAP-05 requirement IDs in REQUIREMENTS.md as the single source of truth for the phase.

Output: A new `slam` Docker service deployable via `docker compose up slam` on the Pi, a committed `config/slam_toolbox_params.yaml` overriding upstream defaults with documented rationale, MAP-01..MAP-05 in REQUIREMENTS.md, and an SSH-verified /map topic + TF tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-live-mapping-slam-toolbox/04-RESEARCH.md
@docker-compose.yml
@config/cyclonedds.xml
@config/ekf.yaml
@config/mower_nav_launch.py
@docker/nav/Dockerfile
@docker/lidar/Dockerfile

<interfaces>
<!-- Existing docker-compose x-ros-common anchor that this plan extends. -->
<!-- (see docker-compose.yml lines 1-10) -->

x-ros-common: &ros-common
  restart: unless-stopped
  network_mode: host
  ipc: host
  pid: host
  environment:
    - ROS_DOMAIN_ID=${ROS_DOMAIN_ID:-0}
  volumes:
    - ./config:/config:ro

<!-- Published frames the slam node must be able to look up via TF -->
<!-- Phase 2 SUMMARY: base_link -> laser_frame (static, from mower_nav_launch.py) -->
<!-- mower-nav (EKF) publishes: odom -> base_link with publish_tf: true in config/ekf.yaml -->

<!-- slam_toolbox WILL publish: map -> odom (at transform_publish_period) -->
<!-- slam_toolbox WILL NOT subscribe to /odom; it reads odom via TF lookup only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MAP-01..MAP-05 to REQUIREMENTS.md + traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Under the "### Web Visualization" block add a new section "### Live Mapping (Phase 4)" with five bullets:
      - MAP-01: `slam` Docker service starts cleanly via `docker compose up slam`; image pinned (not `latest`); inherits `*ros-common` (`network_mode/ipc/pid: host`, CycloneDDS env, /config mount).
      - MAP-02: `ros2 topic hz /map` reports >= 0.5 Hz under indoor scan for 60 s; `ros2 topic echo /map --once` returns a populated `nav_msgs/OccupancyGrid`.
      - MAP-03: TF tree contains `map -> odom -> base_link -> laser_frame` without warnings; `map -> odom` published by slam_toolbox (not static); verified via `ros2 run tf2_tools view_frames`.
      - MAP-04: `/lidar` page renders the occupancy grid bitmap under the live polar scan within 5 s of opening; map persists after `/scan` stops; "Reset Map" button clears it via `/slam_toolbox/reset` service.
      - MAP-05: With the robot physically stationary for 60 s indoors, the map does NOT drift; reference wall stays within ±5 cm in the bitmap.

    In the Coverage block, update totals: v1 requirements from 16 -> 21, mapped to phases 21, unmapped 0.

    In the Traceability table append five rows: MAP-01/02/03/05 -> Phase 4 -> Pending (MAP-04 also Phase 4, Pending). Order by ID.

    Do NOT modify existing rows or the "Out of Scope" / "v2" blocks.
  </action>
  <verify>
    <automated>grep -E '^- \[ \] \*\*MAP-0[1-5]\*\*' .planning/REQUIREMENTS.md | wc -l | grep -q '^5$' && grep -E '^\| MAP-0[1-5] \| Phase 4 \| Pending \|' .planning/REQUIREMENTS.md | wc -l | grep -q '^5$'</automated>
  </verify>
  <done>Five MAP-XX bullets present; five traceability rows present; coverage totals updated to 21/21/0.</done>
</task>

<task type="auto">
  <name>Task 2: Create docker/slam/Dockerfile</name>
  <files>docker/slam/Dockerfile</files>
  <action>
    Create `docker/slam/Dockerfile` following the pattern from RESEARCH.md "Standard Stack / Installation strategy" (04-RESEARCH.md lines ~71-86):

    ```dockerfile
    FROM ros:humble-ros-base

    RUN apt-get update && apt-get install -y --no-install-recommends \
        ros-humble-slam-toolbox \
        ros-humble-rmw-cyclonedds-cpp \
     && rm -rf /var/lib/apt/lists/*

    ENV RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
    ENV CYCLONEDDS_URI=file:///config/cyclonedds.xml

    CMD ["ros2", "launch", "slam_toolbox", "online_async_launch.py", \
         "slam_params_file:=/config/slam_toolbox_params.yaml", \
         "use_sim_time:=false"]
    ```

    The `use_sim_time:=false` override is mandatory — RESEARCH Pitfall #7 (line ~334): upstream launch file defaults `use_sim_time: true`, which hangs waiting for `/clock`. Do NOT omit.

    Do NOT copy `cyclonedds.xml` into the image — the `*ros-common` anchor mounts `./config:/config:ro` at runtime, matching how `docker/nav` and `docker/lidar` do it.
  </action>
  <verify>
    <automated>test -f docker/slam/Dockerfile && grep -q 'ros-humble-slam-toolbox' docker/slam/Dockerfile && grep -q 'use_sim_time:=false' docker/slam/Dockerfile && grep -q 'CYCLONEDDS_URI=file:///config/cyclonedds.xml' docker/slam/Dockerfile</automated>
  </verify>
  <done>Dockerfile exists; installs slam_toolbox + cyclonedds apt packages; CMD launches online_async with params file + use_sim_time:=false.</done>
</task>

<task type="auto">
  <name>Task 3: Create config/slam_toolbox_params.yaml with project overrides</name>
  <files>config/slam_toolbox_params.yaml</files>
  <action>
    Create `config/slam_toolbox_params.yaml` copying the full upstream `mapper_params_online_async.yaml` from slam_toolbox humble branch (RESEARCH.md lines ~155-216 has the verbatim content with our overrides already marked). Apply the following OVERRIDEs vs upstream and leave every other key at upstream default. Each override must have an inline `# OVERRIDE: <reason>` comment so future-us knows why:

      - `base_frame: base_link`  (OVERRIDE: upstream is base_footprint; our TF tree has no base_footprint — RESEARCH Pitfall #2)
      - `odom_frame: odom`  (explicit; upstream default but worth pinning)
      - `map_frame: map`    (explicit; upstream default)
      - `scan_topic: /scan` (explicit)
      - `use_map_saver: false`  (OVERRIDE: no disk persistence in v0 — deferred idea)
      - `transform_publish_period: 0.05`  (OVERRIDE: 20 Hz map->odom is enough; cheaper than upstream 50 Hz)
      - `map_update_interval: 2.0`  (OVERRIDE: 0.5 Hz publish — satisfies SC#1, halves CPU vs upstream 5.0s default which is 0.2 Hz — wait, upstream default is 5.0; check — research line 178 says 2.0 = 0.5 Hz; keep 2.0 to satisfy MAP-02 SC threshold)
      - `min_laser_range: 0.05`  (OVERRIDE: LD19 minimum)
      - `max_laser_range: 12.0`  (OVERRIDE: LD19 datasheet practical max per RESEARCH)
      - `transform_timeout: 1.0`  (OVERRIDE: slack for IMU-only EKF jitter — RESEARCH Pitfall #1)
      - `minimum_travel_distance: 0.1`  (OVERRIDE: mower moves <= 0.3 m/s — RESEARCH Pitfall #3)
      - `minimum_travel_heading: 0.1`  (OVERRIDE: ~5.7° — same reason)
      - `do_loop_closing: false`  (OVERRIDE: Pi 4 CPU budget + v0 simplicity — RESEARCH Pitfall #6)
      - `use_scan_matching: true` (explicit; core requirement for scan-matching-without-wheel-odom)
      - `use_scan_barycenter: true` (explicit)
      - `mode: mapping` (explicit; upstream default but pin it — RESEARCH scope notes rule out localization mode for v0)
      - `debug_logging: false` (explicit)

    Ceres plugin block, correlation block, scan matcher block, and all other keys: copy upstream defaults verbatim (values given in RESEARCH.md lines 156-216).

    Add a header comment block at the top of the file:
    ```yaml
    # slam_toolbox params — Phase 4 Plan 04-01
    # Source: upstream humble mapper_params_online_async.yaml (verbatim) + project OVERRIDEs
    # See .planning/phases/04-live-mapping-slam-toolbox/04-RESEARCH.md for per-key rationale
    ```
  </action>
  <verify>
    <automated>test -f config/slam_toolbox_params.yaml && python3 -c "import yaml; d=yaml.safe_load(open('config/slam_toolbox_params.yaml'))['slam_toolbox']['ros__parameters']; assert d['base_frame']=='base_link', d.get('base_frame'); assert d['do_loop_closing'] is False; assert d['use_map_saver'] is False; assert abs(d['transform_timeout']-1.0)<1e-6; assert abs(d['map_update_interval']-2.0)<1e-6; assert abs(d['max_laser_range']-12.0)<1e-6; assert abs(d['minimum_travel_distance']-0.1)<1e-6; assert abs(d['minimum_travel_heading']-0.1)<1e-6; assert d['use_scan_matching'] is True; print('OK')"</automated>
  </verify>
  <done>YAML parses; every required OVERRIDE present at expected value; file has header comment referencing RESEARCH.md.</done>
</task>

<task type="auto">
  <name>Task 4: Append slam service to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
    Append a new `slam` service to `docker-compose.yml` directly after the `lidar` service (before the `volumes:` block at the end), following the `*ros-common` inheritance pattern used by every other ROS container:

    ```yaml
      # ============================================
      # SLAM — slam_toolbox online async mapping
      # Consumes /scan + (odom -> base_link TF), publishes /map + (map -> odom TF)
      # ============================================
      slam:
        <<: *ros-common
        build:
          context: ./docker/slam
        image: ghcr.io/danyial/mowbot/slam:humble-2.6.10
        container_name: mower-slam
        depends_on:
          lidar:
            condition: service_started
          nav:
            condition: service_started
          micro-ros-agent:
            condition: service_started
        command: >
          ros2 launch slam_toolbox online_async_launch.py
          slam_params_file:=/config/slam_toolbox_params.yaml
          use_sim_time:=false
    ```

    The `image:` tag is pinned to `humble-2.6.10` (NOT `latest`) per MAP-01 and RESEARCH.md "Standard Stack" — slam_toolbox 2.6.10 is the current Humble apt version. The `build:` block is present so `docker compose build slam` works locally on the Pi before any registry push.

    `depends_on` ordering (lidar + nav + micro-ros-agent → slam) is load-bearing — RESEARCH Pitfall #1: slam_toolbox needs `odom -> base_link` TF (from nav's EKF) and `/scan` (from lidar) present at startup, else it logs "Failed to compute odom pose" forever.

    Do NOT modify existing services, the `x-ros-common` anchor, or the `volumes:` block. Do NOT add a healthcheck — no other ROS service has one.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml config --services | grep -qx slam && python3 -c "import yaml; d=yaml.safe_load(open('docker-compose.yml'))['services']['slam']; assert d['container_name']=='mower-slam'; assert 'humble-2.6.10' in d['image']; assert 'latest' not in d['image']; assert set(d['depends_on'].keys())=={'lidar','nav','micro-ros-agent'}; assert 'use_sim_time:=false' in d['command']; assert 'slam_params_file:=/config/slam_toolbox_params.yaml' in d['command']; print('OK')"</automated>
  </verify>
  <done>`docker compose config` parses clean; slam service present with pinned image, correct depends_on, launch command with both `slam_params_file` and `use_sim_time:=false` args.</done>
</task>

<task type="auto">
  <name>Task 5: Deploy + build + up on the Pi, collect verification evidence</name>
  <files>.planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log</files>
  <action>
    All commands run from the dev machine. Project on Pi is at `~/mowbot`. SSH: `pi@10.10.40.23`, password-auth authorized per MEMORY. Use `sshpass -p 'password'` OR rely on the user's existing SSH session (MEMORY: ssh-pi-access authorizes password-based SSH — but prefer `scp` + key if set up).

    Steps (write all evidence into `04-01-DEPLOY.log` as you go, tee stdout+stderr):

    1. `scp docker-compose.yml pi@10.10.40.23:~/mowbot/docker-compose.yml`
    2. `scp config/slam_toolbox_params.yaml pi@10.10.40.23:~/mowbot/config/slam_toolbox_params.yaml`
    3. `ssh pi@10.10.40.23 'mkdir -p ~/mowbot/docker/slam'`
    4. `scp docker/slam/Dockerfile pi@10.10.40.23:~/mowbot/docker/slam/Dockerfile`
    5. On Pi: `cd ~/mowbot && docker compose build slam` — capture full output (first build will apt-install slam_toolbox, expect several minutes).
    6. On Pi: `docker compose up -d slam` — capture output.
    7. Wait ~10 s, then on Pi: `docker compose ps slam` — must show state `running` (not `restarting`). If `restarting`, STOP and dump `docker compose logs --tail 100 slam` into the log — this is a blocking regression, do not proceed to the checkpoint.
    8. On Pi, from inside the nav container (same DDS plane): `docker compose exec nav bash -lc 'ros2 topic hz /map'` — let it run 30 s, Ctrl-C, log the reported Hz (must be ≥ 0.4, ideally ~0.5).
    9. On Pi: `docker compose exec nav bash -lc 'ros2 topic echo /map --once'` — log the first 20 lines of the OccupancyGrid (confirms populated `info.width/height/resolution`, non-empty `data`).
    10. On Pi: `docker compose exec nav bash -lc 'ros2 run tf2_ros tf2_echo map odom 2>&1 | head -20'` — exit code 0 within 5 s, prints a Transform block.
    11. On Pi: `docker compose exec nav bash -lc 'cd /tmp && timeout 10 ros2 run tf2_tools view_frames; ls /tmp/frames.*'` — a `frames.pdf` or `frames.gv` file is generated; scp it back into `.planning/phases/04-live-mapping-slam-toolbox/04-01-tf-frames.pdf` (or `.gv` if pdf generation is missing the graphviz pipeline) so the reviewer can inspect the tree.

    Do NOT run the stationary 60 s drift test (MAP-05) yet — that is the human-verify checkpoint (Task 6). The goal of this task is to prove /map is published, not that it is drift-free.

    If any step fails, STOP and surface the failure + relevant log excerpt. Do not mark the plan complete.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log && grep -q 'average rate:' .planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log && grep -q 'nav_msgs/msg/OccupancyGrid\|width:\|resolution:' .planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log && (test -f .planning/phases/04-live-mapping-slam-toolbox/04-01-tf-frames.pdf || test -f .planning/phases/04-live-mapping-slam-toolbox/04-01-tf-frames.gv)</automated>
  </verify>
  <done>DEPLOY.log contains hz output ≥ 0.4, OccupancyGrid echo with populated metadata, tf2_echo map->odom success; TF frames dump saved locally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human verification — SC#1/#2/#4 SSH verification</name>
  <what-built>
    - `docker/slam/Dockerfile`, `config/slam_toolbox_params.yaml`, `docker-compose.yml` with pinned `slam` service, MAP-01..MAP-05 in REQUIREMENTS.md.
    - Deployed to `pi@10.10.40.23:~/mowbot`, `mower-slam` container running, `/map` publishing, `map -> odom -> base_link -> laser_frame` TF tree present.
    - Deploy evidence captured in `04-01-DEPLOY.log` + `04-01-tf-frames.{pdf,gv}`.
  </what-built>
  <how-to-verify>
    The orchestrator should SSH into the Pi and run these three probes. Each is a phase SC gate:

    **SC #1 (MAP-02) — /map publish rate:**
    ```
    ssh pi@10.10.40.23 'cd ~/mowbot && docker compose exec nav bash -lc "timeout 60 ros2 topic hz /map"'
    ```
    Expect: `average rate: 0.4` to `0.6` Hz over a 60 s window (we target 0.5 via `map_update_interval: 2.0`).

    **SC #2 (MAP-03) — TF tree clean, map->odom not static:**
    ```
    ssh pi@10.10.40.23 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 run tf2_ros tf2_echo map odom"'
    ```
    Expect: Transform block printing within ~1 s, timestamp advances between prints (non-static; `/tf` not `/tf_static`). Also:
    ```
    ssh pi@10.10.40.23 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 run tf2_ros tf2_echo odom base_link"'
    ssh pi@10.10.40.23 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 run tf2_ros tf2_echo base_link laser_frame"'
    ```
    Both must succeed — confirms full `map -> odom -> base_link -> laser_frame` chain.

    **SC #4 (MAP-05) — stationary 60 s no-drift:**
    With the robot physically stationary and LD19 mounted indoors with a reference wall visible:
    ```
    ssh pi@10.10.40.23 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 topic echo /map --once > /tmp/map_t0.txt; sleep 60; ros2 topic echo /map --once > /tmp/map_t60.txt; diff <(head -200 /tmp/map_t0.txt) <(head -200 /tmp/map_t60.txt) | wc -l"'
    ```
    Expect: diff ≤ a handful of lines (timestamp + seq only). The reference-wall cell should stay within ±1 cell (±5 cm at `resolution: 0.05`) across the two snapshots. This is the "scan matcher refuses to fire below motion thresholds" behavior documented in RESEARCH Pitfall #3.

    Additional cross-check (optional, catches Pitfall #6 regressions):
    ```
    ssh pi@10.10.40.23 'htop -b -n 1 | grep -E "async_slam_toolbox|slam_toolbox"'
    ```
    Expect: < 80% sustained CPU on a single core; no 200%+ bursts (which would indicate loop closure is on — a regression since we set `do_loop_closing: false`).
  </how-to-verify>
  <resume-signal>Type "approved" after all three SSH probes pass, or describe the failure (include full `docker compose logs slam` output).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN -> rosbridge | Any host on the LAN can call `/slam_toolbox/reset` via WS. Inherited from existing rosbridge trust model; documented, not enforced in Phase 4. |
| slam_toolbox container -> DDS plane | slam_toolbox joins the host-network DDS plane with full publish rights (can publish `/map`, TF). Container runs as root; no user namespace. Same posture as every other ROS container in this stack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | slam_toolbox OccupancyGrid published to DDS | accept | LAN-trust model, same as `/scan`, `/odom`, `/fix`. No new attack surface vs Phase 2. |
| T-04-01-02 | Denial of Service | `do_loop_closing: true` could pin Pi CPU | mitigate | `do_loop_closing: false` in params YAML (Task 3) — RESEARCH Pitfall #6. |
| T-04-01-03 | Denial of Service | `use_sim_time: true` default hangs node forever | mitigate | Explicit `use_sim_time:=false` in Dockerfile CMD AND docker-compose command (Tasks 2 + 4) — belt-and-braces per RESEARCH Pitfall #7. |
| T-04-01-04 | Information Disclosure | /map published unauthenticated | accept | LAN-only; existing posture. No PII. |
| T-04-01-05 | Repudiation | No audit of who triggered `/slam_toolbox/reset` | accept | Out of scope for v0 (Plan B wires the service; audit is a v2 concern). |
</threat_model>

<verification>
- `docker compose config` parses clean with the new `slam` service
- `docker compose ps slam` shows `running` (not `restarting`) on the Pi
- `ros2 topic hz /map` ≥ 0.4 Hz over 60 s
- `ros2 topic echo /map --once` prints populated `nav_msgs/OccupancyGrid`
- `ros2 run tf2_ros tf2_echo map odom` + `odom base_link` + `base_link laser_frame` all succeed; `map -> odom` updates (non-static)
- Stationary 60 s test: OccupancyGrid snapshot at t=0 and t=60 differs only in timestamp (reference wall cell unchanged within ±1 cell)
- `REQUIREMENTS.md` contains MAP-01..MAP-05 with updated coverage totals
</verification>

<success_criteria>
- Phase 4 SC #1 (container up + /map publishing at ≥ 0.5 Hz): PASS via Task 5 hz check + Task 6 SSH probe.
- Phase 4 SC #2 (TF tree map -> odom -> base_link -> laser_frame, map->odom from slam_toolbox not static): PASS via Task 5 `view_frames` + Task 6 three-segment `tf2_echo` chain.
- Phase 4 SC #4 (stationary 60 s no-drift): PASS via Task 6 diff of t=0 vs t=60 OccupancyGrid snapshots.
- Plan 04-02 is unblocked: `/map` is present on the DDS plane and accessible via rosbridge.
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-mapping-slam-toolbox/04-01-SUMMARY.md` including: what shipped, overrides applied vs upstream (with rationale + RESEARCH line refs), deploy evidence (hz, tf tree, stationary diff), open items for Plan 04-02, and a self-check listing every file modified + the commit SHA.
</output>
