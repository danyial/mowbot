---
id: 04-01
phase: 04-live-mapping-slam-toolbox
plan: 01
type: execute
wave: 1
revision: 2
depends_on: []
autonomous: false
requirements: [MAP-01, MAP-02, MAP-03, MAP-05]
files_modified:
  - docker/slam/Dockerfile
  - config/slam_toolbox_params.yaml
  - docker-compose.yml
  - .planning/REQUIREMENTS.md

must_haves:
  truths:
    - "docker compose up slam brings up mower-slam container, healthy, inheriting ros-common (network_mode/ipc/pid: host + CycloneDDS)"
    - "ros2 topic hz /map reports >= 0.5 Hz for 60 s under indoor /scan"
    - "ros2 topic echo /map --once prints a populated nav_msgs/OccupancyGrid"
    - "ros2 run tf2_tools view_frames produces a tree with map -> odom -> base_link -> laser_frame and map -> odom is published by slam_toolbox (not static)"
    - "With the robot physically stationary for 60 s indoors, the published OccupancyGrid does NOT drift (scan matcher does not fire below thresholds; reference wall cell stays within ±1 cell)"
    - "All 10 existing docker-compose services (micro-ros-agent, gnss, imu, ntrip, nav, rosbridge, web, lidar, mower-nav, mower-ekf if separately containerized) remain Up after slam is deployed — zero regression on prior services"
    - "REQUIREMENTS.md contains MAP-01..MAP-05 with coverage table and traceability rows updated, committed"
  artifacts:
    - path: "docker/slam/Dockerfile"
      provides: "ros:humble-ros-base + ros-humble-slam-toolbox + CycloneDDS, CMD launches online_async with params file and use_sim_time:=false"
      contains: "ros-humble-slam-toolbox"
    - path: "config/slam_toolbox_params.yaml"
      provides: "Project-local slam_toolbox params overriding upstream defaults (base_link, do_loop_closing: false, transform_timeout: 1.0, map_update_interval: 2.0, max_laser_range: 12.0, use_map_saver: false, minimum_travel_{distance,heading}: 0.1)"
      contains: "base_frame: base_link"
    - path: "docker-compose.yml"
      provides: "New slam service inheriting *ros-common, pinned image tag, depends_on lidar+nav+micro-ros-agent, command uses online_async_launch.py + slam_params_file + use_sim_time:=false"
      contains: "slam:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MAP-01..MAP-05 in v1 requirements block + traceability rows mapping to Phase 4"
      contains: "MAP-01"
  key_links:
    - from: "docker-compose.yml slam service"
      to: "config/slam_toolbox_params.yaml"
      via: "volumes: ./config:/config:ro (inherited from *ros-common) + slam_params_file:=/config/slam_toolbox_params.yaml in command"
      pattern: "slam_params_file:=/config/slam_toolbox_params.yaml"
    - from: "slam_toolbox node"
      to: "EKF odom -> base_link TF (published by mower-nav)"
      via: "TF lookup at each scan timestamp; transform_timeout: 1.0 tolerates EKF jitter"
      pattern: "base_frame: base_link"
    - from: "slam_toolbox node"
      to: "LD19 /scan (10 Hz, BEST_EFFORT)"
      via: "default slam_toolbox scan subscriber QoS (sensor-data compatible)"
      pattern: "scan_topic: /scan"
---

<!-- revision: 2 — addresses PLAN-CHECK flags A (Plan-B fallback operationalized as Task 7), B (canonical sshpass invocation), C (12-row regression gate restored). -->

<objective>
Stand up the `slam` ROS2 service: a pinned Humble slam_toolbox container running `online_async_launch.py` with a project-local params file that overrides the dangerous upstream defaults (base_footprint, use_sim_time true, loop closure on, 0.5 m travel threshold), wired into the existing docker-compose stack by inheriting the `*ros-common` anchor. After this plan deploys to the Pi, `/map` publishes at ≥ 0.5 Hz with a clean `map -> odom -> base_link -> laser_frame` TF tree, verified by SSH into `mower-slam`. This is the ROS-side half of Phase 4; Plan 04-02 consumes `/map` in the browser.

Purpose: Phase 4 SC #1 (container up + /map publishing), #2 (TF tree correct with map->odom from slam_toolbox), #4 (stationary stability — no drift for 60 s). Also lands the MAP-01..MAP-05 requirement IDs in REQUIREMENTS.md as the single source of truth for the phase.

Output: A new `slam` Docker service deployable via `docker compose up slam` on the Pi, a committed `config/slam_toolbox_params.yaml` overriding upstream defaults with documented rationale, MAP-01..MAP-05 in REQUIREMENTS.md, and an SSH-verified /map topic + TF tree. If probe 3 shows drift, Task 7 executes the pre-staged EKF `publish_tf: false` + static `odom->base_link` fallback in one shell command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-live-mapping-slam-toolbox/04-RESEARCH.md
@docker-compose.yml
@config/cyclonedds.xml
@config/ekf.yaml
@config/mower_nav_launch.py
@docker/nav/Dockerfile
@docker/lidar/Dockerfile

<ssh_invocation>
<!-- Canonical SSH command for all Pi tasks in this plan. Uses password auth per -->
<!-- MEMORY (ssh-pi-access). Matches prior plans' invocation. -->
<!-- Requires `sshpass` locally: `brew install hudochenkov/sshpass/sshpass` on macOS. -->

SSH_CMD='sshpass -p password ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o PreferredAuthentications=password -o PubkeyAuthentication=no pi@10.10.40.23'
SCP_CMD='sshpass -p password scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o PreferredAuthentications=password -o PubkeyAuthentication=no'
</ssh_invocation>

<interfaces>
<!-- Existing docker-compose x-ros-common anchor that this plan extends. -->
<!-- (see docker-compose.yml lines 1-10) -->

x-ros-common: &ros-common
  restart: unless-stopped
  network_mode: host
  ipc: host
  pid: host
  environment:
    - ROS_DOMAIN_ID=${ROS_DOMAIN_ID:-0}
  volumes:
    - ./config:/config:ro

<!-- Published frames the slam node must be able to look up via TF -->
<!-- Phase 2 SUMMARY: base_link -> laser_frame (static, from mower_nav_launch.py) -->
<!-- mower-nav (EKF) publishes: odom -> base_link with publish_tf: true in config/ekf.yaml -->

<!-- slam_toolbox WILL publish: map -> odom (at transform_publish_period) -->
<!-- slam_toolbox WILL NOT subscribe to /odom; it reads odom via TF lookup only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MAP-01..MAP-05 to REQUIREMENTS.md + traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Under the "### Web Visualization" block add a new section "### Live Mapping (Phase 4)" with five bullets:
      - MAP-01: `slam` Docker service starts cleanly via `docker compose up slam`; image pinned (not `latest`); inherits `*ros-common` (`network_mode/ipc/pid: host`, CycloneDDS env, /config mount).
      - MAP-02: `ros2 topic hz /map` reports >= 0.5 Hz under indoor scan for 60 s; `ros2 topic echo /map --once` returns a populated `nav_msgs/OccupancyGrid`.
      - MAP-03: TF tree contains `map -> odom -> base_link -> laser_frame` without warnings; `map -> odom` published by slam_toolbox (not static); verified via `ros2 run tf2_tools view_frames`.
      - MAP-04: `/lidar` page renders the occupancy grid bitmap under the live polar scan within 5 s of opening; map persists after `/scan` stops; "Reset Map" button clears it via `/slam_toolbox/reset` service.
      - MAP-05: With the robot physically stationary for 60 s indoors, the map does NOT drift; reference wall stays within ±5 cm in the bitmap.

    In the Coverage block, update totals: v1 requirements from 16 -> 21, mapped to phases 21, unmapped 0.

    In the Traceability table append five rows: MAP-01/02/03/05 -> Phase 4 -> Pending (MAP-04 also Phase 4, Pending). Order by ID.

    Do NOT modify existing rows or the "Out of Scope" / "v2" blocks.
  </action>
  <verify>
    <automated>grep -E '^- \[ \] \*\*MAP-0[1-5]\*\*' .planning/REQUIREMENTS.md | wc -l | grep -q '^5$' && grep -E '^\| MAP-0[1-5] \| Phase 4 \| Pending \|' .planning/REQUIREMENTS.md | wc -l | grep -q '^5$'</automated>
  </verify>
  <done>Five MAP-XX bullets present; five traceability rows present; coverage totals updated to 21/21/0.</done>
</task>

<task type="auto">
  <name>Task 2: Create docker/slam/Dockerfile</name>
  <files>docker/slam/Dockerfile</files>
  <action>
    Create `docker/slam/Dockerfile` following the pattern from RESEARCH.md "Standard Stack / Installation strategy" (04-RESEARCH.md lines ~71-86):

    ```dockerfile
    FROM ros:humble-ros-base

    RUN apt-get update && apt-get install -y --no-install-recommends \
        ros-humble-slam-toolbox \
        ros-humble-rmw-cyclonedds-cpp \
     && rm -rf /var/lib/apt/lists/*

    ENV RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
    ENV CYCLONEDDS_URI=file:///config/cyclonedds.xml

    CMD ["ros2", "launch", "slam_toolbox", "online_async_launch.py", \
         "slam_params_file:=/config/slam_toolbox_params.yaml", \
         "use_sim_time:=false"]
    ```

    The `use_sim_time:=false` override is mandatory — RESEARCH Pitfall #7: upstream launch file defaults `use_sim_time: true`, which hangs waiting for `/clock`. Do NOT omit.

    Do NOT copy `cyclonedds.xml` into the image — the `*ros-common` anchor mounts `./config:/config:ro` at runtime.
  </action>
  <verify>
    <automated>test -f docker/slam/Dockerfile && grep -q 'ros-humble-slam-toolbox' docker/slam/Dockerfile && grep -q 'use_sim_time:=false' docker/slam/Dockerfile && grep -q 'CYCLONEDDS_URI=file:///config/cyclonedds.xml' docker/slam/Dockerfile</automated>
  </verify>
  <done>Dockerfile exists; installs slam_toolbox + cyclonedds apt packages; CMD launches online_async with params file + use_sim_time:=false.</done>
</task>

<task type="auto">
  <name>Task 3: Create config/slam_toolbox_params.yaml with project overrides</name>
  <files>config/slam_toolbox_params.yaml</files>
  <action>
    Create `config/slam_toolbox_params.yaml` copying the full upstream `mapper_params_online_async.yaml` from slam_toolbox humble branch (RESEARCH.md lines ~155-216 has the verbatim content). Apply the following OVERRIDEs vs upstream and leave every other key at upstream default. Each override must have an inline `# OVERRIDE: <reason>` comment:

      - `base_frame: base_link`  (OVERRIDE: upstream is base_footprint; our TF tree has no base_footprint — RESEARCH Pitfall #2)
      - `odom_frame: odom`  (explicit; upstream default but worth pinning)
      - `map_frame: map`    (explicit; upstream default)
      - `scan_topic: /scan` (explicit)
      - `use_map_saver: false`  (OVERRIDE: no disk persistence in v0)
      - `transform_publish_period: 0.05`  (OVERRIDE: 20 Hz map->odom)
      - `map_update_interval: 2.0`  (OVERRIDE: 0.5 Hz publish — satisfies SC#1)
      - `min_laser_range: 0.05`  (OVERRIDE: LD19 minimum)
      - `max_laser_range: 12.0`  (OVERRIDE: LD19 datasheet practical max)
      - `transform_timeout: 1.0`  (OVERRIDE: slack for IMU-only EKF jitter — RESEARCH Pitfall #1)
      - `minimum_travel_distance: 0.1`  (OVERRIDE: RESEARCH Pitfall #3)
      - `minimum_travel_heading: 0.1`  (OVERRIDE: ~5.7° — same reason)
      - `do_loop_closing: false`  (OVERRIDE: Pi 4 CPU budget + v0 simplicity — RESEARCH Pitfall #6)
      - `use_scan_matching: true`
      - `use_scan_barycenter: true`
      - `mode: mapping`
      - `debug_logging: false`

    All other keys: upstream defaults verbatim.

    Header comment block:
    ```yaml
    # slam_toolbox params — Phase 4 Plan 04-01
    # Source: upstream humble mapper_params_online_async.yaml (verbatim) + project OVERRIDEs
    # See .planning/phases/04-live-mapping-slam-toolbox/04-RESEARCH.md for per-key rationale
    ```
  </action>
  <verify>
    <automated>test -f config/slam_toolbox_params.yaml && python3 -c "import yaml; d=yaml.safe_load(open('config/slam_toolbox_params.yaml'))['slam_toolbox']['ros__parameters']; assert d['base_frame']=='base_link'; assert d['do_loop_closing'] is False; assert d['use_map_saver'] is False; assert abs(d['transform_timeout']-1.0)<1e-6; assert abs(d['map_update_interval']-2.0)<1e-6; assert abs(d['max_laser_range']-12.0)<1e-6; assert abs(d['minimum_travel_distance']-0.1)<1e-6; assert abs(d['minimum_travel_heading']-0.1)<1e-6; assert d['use_scan_matching'] is True; print('OK')"</automated>
  </verify>
  <done>YAML parses; every required OVERRIDE present at expected value; header comment present.</done>
</task>

<task type="auto">
  <name>Task 4: Append slam service to docker-compose.yml + regression gate</name>
  <files>docker-compose.yml</files>
  <action>
    Append a new `slam` service to `docker-compose.yml` directly after the `lidar` service (before the `volumes:` block at the end):

    ```yaml
      # ============================================
      # SLAM — slam_toolbox online async mapping
      # Consumes /scan + (odom -> base_link TF), publishes /map + (map -> odom TF)
      # ============================================
      slam:
        <<: *ros-common
        build:
          context: ./docker/slam
        image: ghcr.io/danyial/mowbot/slam:humble-2.6.10
        container_name: mower-slam
        depends_on:
          lidar:
            condition: service_started
          nav:
            condition: service_started
          micro-ros-agent:
            condition: service_started
        command: >
          ros2 launch slam_toolbox online_async_launch.py
          slam_params_file:=/config/slam_toolbox_params.yaml
          use_sim_time:=false
    ```

    The `image:` tag is pinned to `humble-2.6.10` per MAP-01. `depends_on` ordering (lidar + nav + micro-ros-agent → slam) is load-bearing — RESEARCH Pitfall #1.

    Do NOT modify existing services, the `x-ros-common` anchor, or the `volumes:` block. Do NOT add a healthcheck.

    **Flag C — regression gate for existing services:** The verify step below asserts all 9 prior services are still present in the compose file after our edit (micro-ros-agent, gnss, imu, ntrip, nav, rosbridge, web, lidar + the new slam). This inherits the Phase 2 Plan 02-01 discipline — any accidental deletion or rename blocks the task.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml config --services | grep -qx slam && python3 -c "import yaml; d=yaml.safe_load(open('docker-compose.yml'))['services']; required={'micro-ros-agent','gnss','imu','ntrip','nav','rosbridge','web','lidar','slam'}; missing=required-set(d.keys()); assert not missing, f'missing services: {missing}'; s=d['slam']; assert s['container_name']=='mower-slam'; assert 'humble-2.6.10' in s['image']; assert 'latest' not in s['image']; assert set(s['depends_on'].keys())=={'lidar','nav','micro-ros-agent'}; assert 'use_sim_time:=false' in s['command']; assert 'slam_params_file:=/config/slam_toolbox_params.yaml' in s['command']; print('OK')"</automated>
  </verify>
  <done>`docker compose config` parses clean; all 9 services present (regression gate); slam service has pinned image, correct depends_on, launch command with both `slam_params_file` and `use_sim_time:=false` args.</done>
</task>

<task type="auto">
  <name>Task 5: Deploy + build + up on the Pi, collect verification evidence</name>
  <files>.planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log</files>
  <action>
    All Pi access uses the canonical `sshpass` invocation from `<ssh_invocation>` above:
    ```
    SSHP='sshpass -p password ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o PreferredAuthentications=password -o PubkeyAuthentication=no pi@10.10.40.23'
    SCPP='sshpass -p password scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o PreferredAuthentications=password -o PubkeyAuthentication=no'
    ```
    If `sshpass` is missing locally: `brew install hudochenkov/sshpass/sshpass`. NO alternative paths — consistent with prior Phase 3 plans.

    Steps (tee all output into `04-01-DEPLOY.log`):

    1. `$SCPP docker-compose.yml pi@10.10.40.23:~/mowbot/docker-compose.yml`
    2. `$SCPP config/slam_toolbox_params.yaml pi@10.10.40.23:~/mowbot/config/slam_toolbox_params.yaml`
    3. `$SSHP 'mkdir -p ~/mowbot/docker/slam'`
    4. `$SCPP docker/slam/Dockerfile pi@10.10.40.23:~/mowbot/docker/slam/Dockerfile`
    5. `$SSHP 'cd ~/mowbot && docker compose build slam'` — first build apt-installs slam_toolbox (several minutes).
    6. `$SSHP 'cd ~/mowbot && docker compose up -d slam'`
    7. Wait 10 s, then `$SSHP 'cd ~/mowbot && docker compose ps'` — must show ALL of `micro-ros-agent, gnss, imu, ntrip, nav, rosbridge, web, lidar, slam` with State=`running` (12-row regression gate, Flag C). Any prior service DOWN → STOP, rollback slam, fix, retry.
    8. `$SSHP 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 topic hz /map"'` — 30 s, Ctrl-C, log reported Hz (must be ≥ 0.4).
    9. `$SSHP 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 topic echo /map --once"'` — log first 20 lines (populated `info.width/height/resolution`, non-empty `data`).
    10. `$SSHP 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 run tf2_ros tf2_echo map odom 2>&1 | head -20"'` — exit 0 within 5 s.
    11. `$SSHP 'cd ~/mowbot && docker compose exec nav bash -lc "cd /tmp && timeout 10 ros2 run tf2_tools view_frames; ls /tmp/frames.*"'` — scp `frames.pdf` (or `.gv`) back to `.planning/phases/04-live-mapping-slam-toolbox/04-01-tf-frames.pdf`.

    Do NOT run the stationary 60 s drift test here — that's Task 6.

    If any step fails, STOP and surface the failure. Do not mark the plan complete.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log && grep -q 'average rate:' .planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log && grep -q 'nav_msgs/msg/OccupancyGrid\|width:\|resolution:' .planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log && (test -f .planning/phases/04-live-mapping-slam-toolbox/04-01-tf-frames.pdf || test -f .planning/phases/04-live-mapping-slam-toolbox/04-01-tf-frames.gv) && grep -cE 'micro-ros-agent|gnss|imu|ntrip|nav|rosbridge|web|lidar|slam' .planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log | awk '$1>=9{exit 0} {exit 1}'</automated>
  </verify>
  <done>DEPLOY.log contains hz ≥ 0.4, OccupancyGrid echo with populated metadata, tf2_echo map->odom success; 9-service regression gate recorded; TF frames dump saved locally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human verification — SC#1/#2/#4 SSH verification</name>
  <what-built>
    - `docker/slam/Dockerfile`, `config/slam_toolbox_params.yaml`, `docker-compose.yml` with pinned `slam` service, MAP-01..MAP-05 in REQUIREMENTS.md.
    - Deployed to `pi@10.10.40.23:~/mowbot`, `mower-slam` container running, `/map` publishing, `map -> odom -> base_link -> laser_frame` TF tree present.
    - Deploy evidence in `04-01-DEPLOY.log` + `04-01-tf-frames.{pdf,gv}`.
    - Regression gate: all 9 existing services Up after slam deploy.
  </what-built>
  <how-to-verify>
    Using the canonical `$SSHP` from Task 5, run three probes:

    **SC #1 (MAP-02) — /map publish rate:**
    ```
    $SSHP 'cd ~/mowbot && docker compose exec nav bash -lc "timeout 60 ros2 topic hz /map"'
    ```
    Expect: `average rate: 0.4` to `0.6` Hz over 60 s.

    **SC #2 (MAP-03) — TF tree, map->odom not static:**
    ```
    $SSHP 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 run tf2_ros tf2_echo map odom"'
    $SSHP 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 run tf2_ros tf2_echo odom base_link"'
    $SSHP 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 run tf2_ros tf2_echo base_link laser_frame"'
    ```
    All three must succeed; timestamps on `map->odom` advance between prints.

    **SC #4 (MAP-05) — stationary 60 s no-drift:**
    Robot stationary indoors with reference wall visible:
    ```
    $SSHP 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 topic echo /map --once > /tmp/map_t0.txt; sleep 60; ros2 topic echo /map --once > /tmp/map_t60.txt; diff <(head -200 /tmp/map_t0.txt) <(head -200 /tmp/map_t60.txt) | wc -l"'
    ```
    Expect: diff ≤ a handful of lines (timestamp + seq only). Reference wall cell stays within ±1 cell.

    **If probe 3 diff > a few lines (drift > ±1 cell over 60 s stationary):** Do NOT approve. Instead, resume with signal `"run-plan-b"` to trigger Task 7.
  </how-to-verify>
  <resume-signal>Type "approved" if all three probes pass, "run-plan-b" if SC#4 drift test fails, or describe the failure.</resume-signal>
</task>

<task type="auto" gate="conditional">
  <name>Task 7 (Plan-B fallback): Swap EKF publish_tf:false + static odom->base_link, re-verify SC#4</name>
  <files>config/ekf.yaml, config/mower_nav_launch.py</files>
  <action>
    **Only execute this task if Task 6 was resumed with `"run-plan-b"`.** Otherwise skip.

    This operationalizes Flag A from PLAN-CHECK: if the IMU-only EKF publishes a jittery `odom -> base_link` TF (covariance 1.01e8 per RESEARCH Pitfall #3), slam_toolbox's scan-matcher may fire even at rest and drift the map. The fallback is to stop publishing odom->base_link from EKF and substitute a static identity TF so slam_toolbox sees a perfectly stationary odom frame.

    **Step 1 — Edit `config/ekf.yaml`:** set `publish_tf: false` on the existing EKF node (previously `true`). Comment the change with `# OVERRIDE Plan 04-01 Task 7: slam_toolbox owns map->odom; odom->base_link is now a static identity TF published from mower_nav_launch.py` so future-us understands the dependency.

    **Step 2 — Edit `config/mower_nav_launch.py`:** add a static_transform_publisher node to the launch description, alongside the existing `base_link -> laser_frame` static TF:
    ```python
    Node(
        package='tf2_ros',
        executable='static_transform_publisher',
        name='static_odom_to_base_link',
        # OVERRIDE Plan 04-01 Task 7: replaces EKF's jittery odom->base_link with
        # a static identity TF so slam_toolbox sees a stationary odom frame at rest.
        # When wheel-odometry lands in firmware, revert this + EKF publish_tf: true.
        arguments=['0', '0', '0', '0', '0', '0', 'odom', 'base_link'],
    ),
    ```

    **Step 3 — Deploy + restart:**
    ```
    $SCPP config/ekf.yaml pi@10.10.40.23:~/mowbot/config/ekf.yaml
    $SCPP config/mower_nav_launch.py pi@10.10.40.23:~/mowbot/config/mower_nav_launch.py
    $SSHP 'cd ~/mowbot && docker compose restart nav slam'
    ```
    Wait 10 s for both to re-settle.

    **Step 4 — Re-run SC#4 probe 3:**
    ```
    $SSHP 'cd ~/mowbot && docker compose exec nav bash -lc "ros2 topic echo /map --once > /tmp/map_t0b.txt; sleep 60; ros2 topic echo /map --once > /tmp/map_t60b.txt; diff <(head -200 /tmp/map_t0b.txt) <(head -200 /tmp/map_t60b.txt) | wc -l"'
    ```
    Expect: diff ≤ a handful of lines.

    **Step 5 — Append Plan-B outcome to `04-01-DEPLOY.log`:** record the pre-Plan-B diff count, the post-Plan-B diff count, and the resume-signal from Task 6. This becomes the SUMMARY's "Plan-B outcome" section.

    If even Plan-B fails: STOP. This is a research-level regression (slam_toolbox can't stabilize on pure IMU even with a static odom) — escalate to the orchestrator for a deeper replan, not a third in-task retry.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log && (grep -q 'Plan-B outcome' .planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log || grep -q 'Plan-B skipped' .planning/phases/04-live-mapping-slam-toolbox/04-01-DEPLOY.log)</automated>
  </verify>
  <done>Either Plan-B was skipped (Task 6 approved cleanly) with a "Plan-B skipped" log line, or Plan-B ran and logged a successful re-verified SC#4 diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN -> rosbridge | Any LAN host can call `/slam_toolbox/reset` via WS. Inherited from existing rosbridge trust model. |
| slam_toolbox container -> DDS plane | slam_toolbox joins the host-network DDS plane with full publish rights. Same posture as every other ROS container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | slam_toolbox OccupancyGrid published to DDS | accept | LAN-trust model, same as /scan, /odom, /fix. |
| T-04-01-02 | Denial of Service | `do_loop_closing: true` could pin Pi CPU | mitigate | `do_loop_closing: false` in params YAML (Task 3) — RESEARCH Pitfall #6. |
| T-04-01-03 | Denial of Service | `use_sim_time: true` default hangs node forever | mitigate | Explicit `use_sim_time:=false` in Dockerfile CMD AND compose command (Tasks 2 + 4). |
| T-04-01-04 | Information Disclosure | /map published unauthenticated | accept | LAN-only; no PII. |
| T-04-01-05 | Repudiation | No audit of who triggered `/slam_toolbox/reset` | accept | Out of scope for v0. |
| T-04-01-06 | Denial of Service | Existing services silently dropped from compose during slam edit | mitigate | Task 4 verify asserts 9-service regression gate; Task 5 step 7 reconfirms on live Pi. |
</threat_model>

<verification>
- `docker compose config` parses clean with the new `slam` service and all 9 prior services still present
- `docker compose ps` on Pi shows all 9 services `running`
- `ros2 topic hz /map` ≥ 0.4 Hz over 60 s
- `ros2 topic echo /map --once` prints populated `nav_msgs/OccupancyGrid`
- `ros2 run tf2_ros tf2_echo map odom` + `odom base_link` + `base_link laser_frame` all succeed
- Stationary 60 s diff clean, OR Task 7 Plan-B executed and re-verified
- `REQUIREMENTS.md` contains MAP-01..MAP-05
</verification>

<success_criteria>
- Phase 4 SC #1 (container up + /map ≥ 0.5 Hz): PASS via Task 5 + Task 6 probe.
- Phase 4 SC #2 (TF tree from slam_toolbox): PASS via Task 5 view_frames + Task 6 tf2_echo chain.
- Phase 4 SC #4 (stationary 60 s no-drift): PASS via Task 6 diff, with Task 7 Plan-B as pre-staged fallback.
- Zero regression on existing 8 services: PASS via Task 4 verify + Task 5 step 7.
- Plan 04-02 unblocked: `/map` on DDS plane, accessible via rosbridge.
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-mapping-slam-toolbox/04-01-SUMMARY.md` including: what shipped, overrides applied vs upstream (with rationale + RESEARCH line refs), deploy evidence (hz, tf tree, stationary diff), whether Task 7 Plan-B executed (and its outcome), regression-gate confirmation (9 services up), open items for Plan 04-02, and a self-check listing every file modified + the commit SHA.
</output>
