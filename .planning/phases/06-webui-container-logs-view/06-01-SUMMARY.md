---
phase: 06-webui-container-logs-view
plan: 01
subsystem: testing
tags: [node-test, dockerode, ansi-to-html, tdd, red-state, wave-0, websocket-regression-gate]

requires:
  - phase: "none (Wave 0 scaffolding — no prior phase dependency)"
    provides: "baseline web/server.mjs with single /rosbridge upgrade listener to regression-test against"
provides:
  - "node:test harness wired into web/ via `npm test` → `node --test __tests__/`"
  - "dockerode@4.0.10 + ansi-to-html@0.7.2 (+ @types/dockerode) installed in web/package.json"
  - "4 RED tests specifying the contract for Plan 02 & 03: /rosbridge regression gate, docker-adapter allowlist, 8-byte demux, since-preset translation"
  - "web/tsconfig.json exclude list updated so __tests__/ does not leak into Next.js TS build"
affects: ["06-02-PLAN (production modules must flip these RED tests green)", "06-03-PLAN (ANSI escapeXML verify step)"]

tech-stack:
  added: ["dockerode@^4.0.10", "ansi-to-html@^0.7.2", "@types/dockerode@^3.3.47 (resolved)", "node:test (Node 22 built-in, no new framework)"]
  patterns: ["RED-first Wave 0 scaffolding", "grep-based server.mjs regression gate (single upgrade listener invariant)", "allowlist-as-test (dockerode method surface)"]

key-files:
  created:
    - "web/__tests__/server-upgrade.test.mjs"
    - "web/__tests__/docker-adapter.test.mjs"
    - "web/__tests__/demux.test.mjs"
    - "web/__tests__/since-preset.test.mjs"
  modified:
    - "web/package.json"
    - "web/package-lock.json"
    - "web/tsconfig.json"

key-decisions:
  - "Use Node 22 built-in node:test runner — zero new dev-dep, no vitest/jest migration (RESEARCH.md §Validation)"
  - "Dynamic import('../lib/server/*.js') from .mjs tests — ENOENT on RED is acceptable and literally embedded in VALIDATION.md; Plan 02 will ship .ts → compiled/importable paths"
  - "Unknown demux stream type (type=3) → skip silently (stdout='' + stderr='') — locks graceful degradation"
  - "parseSincePreset('bogus') → null (graceful) vs throwing — locks no-throw API contract for the future route handler"

patterns-established:
  - "Wave 0 RED-first: contract tests land before production code; overall `npm test` exits non-zero until Plan 02 implements the modules — THAT IS THE GATE WORKING"
  - "Single-upgrade-listener regression test via grep on server.mjs source — cheap, deterministic, catches shadowing of /rosbridge"
  - "Docker method-allowlist enforced in the test suite — defense in depth beyond the :ro socket mount"

requirements-completed: [LOGS-01, LOGS-02, LOGS-03, LOGS-04]

duration: 2min
completed: 2026-04-15
---

# Phase 6 Plan 01: Wave 0 test scaffolding + deps Summary

**RED-first test harness + dockerode/ansi-to-html deps landed: 4 node:test files pin the /rosbridge single-listener regression gate, dockerode method allowlist, 8-byte demux contract, and since-preset epoch math — ready for Plan 02 to flip green.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-15
- **Completed:** 2026-04-15
- **Tasks:** 3
- **Files modified:** 7 (3 modified, 4 created)

## Accomplishments
- Installed `dockerode@4.0.10` + `ansi-to-html@0.7.2` + `@types/dockerode@3.3.47` in web/ (no lockfile churn outside these deps' tree)
- Added `npm test` + `npm run test:watch` scripts invoking `node --test __tests__/`
- Landed 4 contract tests (2 currently GREEN on untouched code, 3 currently RED waiting on Plan 02) — the RED state IS the Wave 0 regression gate
- Excluded `__tests__/` from `web/tsconfig.json` so the Next.js Docker build remains unaffected

## Task Commits

Each task was committed atomically:

1. **Task 1: Add deps + test scripts to web/package.json, exclude __tests__ from Next build** — `6752ea0` (chore)
2. **Task 2: Write server-upgrade regression test + docker-adapter allowlist test** — `72d5601` (test)
3. **Task 3: Write demux + since-preset pure-function tests** — `6c26a4f` (test)

**Plan metadata:** _pending final commit after this SUMMARY + STATE updates_

## Files Created/Modified

- `web/package.json` — added dockerode + ansi-to-html deps, @types/dockerode devDep, test/test:watch scripts
- `web/package-lock.json` — regenerated by `npm install` for the three new packages
- `web/tsconfig.json` — added `"__tests__"` to `"exclude"` array alongside node_modules
- `web/__tests__/server-upgrade.test.mjs` — grep regression gate (single upgrade listener + /rosbridge + /logs/stream/ branches)
- `web/__tests__/docker-adapter.test.mjs` — method allowlist (named exports + getContainer facade shape)
- `web/__tests__/demux.test.mjs` — 8-byte-header Docker frame fixture → stdout/stderr split, multi-frame concat, unknown-type skip
- `web/__tests__/since-preset.test.mjs` — 1m/5m/15m/1h/6h/24h preset → `Math.floor(Date.now()/1000) - seconds` ±1s, null + 'bogus' passthrough

## RED / GREEN State at End of Plan

| Test | State | Why |
|------|-------|-----|
| `server-upgrade.test.mjs::has exactly one server.on('upgrade')` | GREEN | Current `web/server.mjs` already has exactly one listener (the v2.1 rosbridge proxy) |
| `server-upgrade.test.mjs::preserves /rosbridge branch` | GREEN | `/rosbridge` string literal already in source |
| `server-upgrade.test.mjs::adds /logs/stream/ branch` | **RED** | Plan 02 must add this branch — gate correctly firing |
| `docker-adapter.test.mjs::allowlist exports` | **RED** | `web/lib/server/docker-adapter.js` does not exist yet — Plan 02 |
| `docker-adapter.test.mjs::facade {inspect,logs,modem}` | **RED** | same — Plan 02 |
| `demux.test.mjs` (3 subtests) | **RED** | `web/lib/server/demux.js` does not exist yet — Plan 02 |
| `since-preset.test.mjs` (3 subtests) | **RED** | `web/lib/server/since-preset.js` does not exist yet — Plan 02 |

**Overall `node --test __tests__/` exit code: non-zero (expected; matches VALIDATION.md §Wave 0 Requirements).**

## Decisions Made
None beyond those already locked by the plan/CONTEXT. Executed plan exactly as written.

## Deviations from Plan

None — plan executed exactly as written. All 4 test files match the specified structure; deps installed at the pinned versions; tsconfig exclude extended without touching compilerOptions or include.

## Issues Encountered

- `npm install` reported 5 vulnerabilities (1 moderate, 4 high) in the full web/ dep tree. These are PRE-EXISTING — not introduced by this plan's new deps. Logging as OUT-OF-SCOPE per the GSD scope-boundary rule; not addressing here.

## Self-Check: PASSED

- `web/package.json` contains dockerode@^4.0.10, ansi-to-html@^0.7.2, @types/dockerode, test/test:watch scripts — VERIFIED via grep (all returned count 1)
- `web/tsconfig.json` excludes `__tests__` — VERIFIED via grep
- All 4 test files exist under `web/__tests__/` — VERIFIED via `ls`
- `grep -c 'server\.on("upgrade"' web/server.mjs` = 1 — VERIFIED
- 3 commits exist in git log: 6752ea0, 72d5601, 6c26a4f — VERIFIED
- `node --test __tests__/` runs and exits non-zero (expected RED for Wave 0) — VERIFIED

## User Setup Required

None — no external service configuration introduced by Wave 0.

## Next Phase Readiness

- Plan 02 can start immediately. It must:
  - Create `web/lib/server/docker-adapter.ts` exporting EXACTLY `{listContainers, getContainer, getEvents}`; `getContainer(id)` must return an object with own keys `{inspect, logs, modem}` and nothing else.
  - Create `web/lib/server/demux.mjs` (or .ts with .js output) exporting `demuxBuffer(buf): {stdout: string, stderr: string}`.
  - Create `web/lib/server/since-preset.mjs` (or .ts with .js output) exporting `parseSincePreset(p): number | null`.
  - Extend `web/server.mjs` with a single new branch inside the existing upgrade handler: `else if (pathname.startsWith("/logs/stream/"))`.
- All 4 Wave 0 test files must flip to GREEN at end of Plan 02 — this is Plan 02's gate.

---
*Phase: 06-webui-container-logs-view*
*Plan: 01*
*Completed: 2026-04-15*
