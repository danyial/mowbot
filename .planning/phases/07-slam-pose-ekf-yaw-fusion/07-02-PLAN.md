---
phase: 07-slam-pose-ekf-yaw-fusion
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - config/ekf.yaml
  - config/slam_toolbox_params.yaml
autonomous: false
requirements: [FUSE-01, FUSE-02, FUSE-04]
user_setup: []

must_haves:
  truths:
    - "config/ekf.yaml contains a new `pose0: <TOPIC>` block (topic name sourced verbatim from Wave 0 handoff) with exactly ONE true entry in `pose0_config` at index 5 (yaw position); all other 14 indices are false. (FUSE-01, D-02)"
    - "config/ekf.yaml `imu0_config` yaw-position index (index 5, 6th entry) is false; yaw-velocity (index 11) remains true alongside roll_vel + pitch_vel. Change landed in the SAME commit as the pose0 addition. (FUSE-01, D-03, D-08)"
    - "config/slam_toolbox_params.yaml contains a new `yaw_covariance_scale:` line with OVERRIDE comment that materializes D-05's conservative ~0.05 rad² target based on Wave 0's native covariance reading. (D-05)"
    - "config/ekf.yaml has a FUSE-04 ADR-style comment block above the `pose0:` line recording: chosen value, rejected values with reasoning, and the D-08 rationale for keeping imu0 yaw_vel=true while disabling yaw-position. (FUSE-04, D-06, D-10)"
    - "`docker compose restart nav` has been executed and `ros2 run tf2_ros tf2_echo map odom` inside `mower-nav` shows `slam_toolbox` as the sole publisher of `map→odom`. No duplicate publishers, no TF cycle. (ROADMAP SC#4, D-04)"
    - "`./scripts/yaw-drift-test.sh 60` PASSes on real hardware with margin (target <0.5° preferred; hard <1.0°). (FUSE-02, D-06, D-16)"
  artifacts:
    - path: "config/ekf.yaml"
      provides: "EKF config with yaw-only pose0 consumption + FUSE-04 ADR comment block"
      contains: "pose0:"
    - path: "config/slam_toolbox_params.yaml"
      provides: "yaw_covariance_scale override that materializes D-05's ~0.05 rad² trust posture"
      contains: "yaw_covariance_scale"
  key_links:
    - from: "config/ekf.yaml pose0_config[5]"
      to: "/odometry/filtered yaw"
      via: "ekf_filter_node Kalman update on scan-rate pose messages"
      pattern: "pose0_config:.*true"
    - from: "config/slam_toolbox_params.yaml yaw_covariance_scale"
      to: "/pose PoseWithCovarianceStamped.pose.covariance[35]"
      via: "slam_toolbox source-side covariance scaling (the only supported override — RESEARCH §Pitfall 1)"
      pattern: "yaw_covariance_scale:\\s*[0-9]"
    - from: "slam_toolbox (mower-slam container)"
      to: "TF map→odom"
      via: "sole publisher per D-04 — EKF stays in odom frame"
      pattern: "transform_publish_period"
---

<objective>
Wave 1 — land all three FUSE-01/02/04 backend changes in a SINGLE commit, restart the nav container,
confirm TF topology, and tune `yaw_covariance_scale` via the drift-test loop until FUSE-02 passes
with margin.

Purpose: D-03 mandates simultaneity — `pose0` addition and `imu0` yaw-position disable MUST ship
together (no intermediate "both enabled" state, which is the correlated-input divergence trap
FUSE-01 exists to prevent). RESEARCH §Pitfall 1 establishes that robot_localization has no
per-sensor variance-override knob, so D-05's "conservative hardcoded ~0.05 rad²" materializes in
`config/slam_toolbox_params.yaml` via `yaw_covariance_scale`, not in `ekf.yaml`. `config/ekf.yaml`
comments are the FUSE-04 deliverable — they document the source-side override decision so a future
reader understands where the number actually lives.

Output: two config edits + a proven-passing drift test + a TF topology sanity check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-slam-pose-ekf-yaw-fusion/07-CONTEXT.md
@.planning/phases/07-slam-pose-ekf-yaw-fusion/07-RESEARCH.md
@.planning/phases/07-slam-pose-ekf-yaw-fusion/07-PATTERNS.md
@.planning/phases/07-slam-pose-ekf-yaw-fusion/07-VALIDATION.md
@.planning/phases/07-slam-pose-ekf-yaw-fusion/07-01-SUMMARY.md
@config/ekf.yaml
@config/slam_toolbox_params.yaml
@config/mower_nav_launch.py
@scripts/yaw-drift-test.sh

<interfaces>
Wave 0 handoff facts (from 07-01-SUMMARY.md §Wave 0 Handoff — read these at plan start):
- `topic_name`: actual slam_toolbox pose topic (`/pose` or `/slam_toolbox/pose`)
- `native_yaw_cov`: covariance[35] magnitude — determines starting `yaw_covariance_scale`
- `preflight_drift_10s`: baseline drift line (likely FAIL pre-fusion; informational)

Target `pose0` block for ekf.yaml (per 07-PATTERNS.md, mirrors existing imu0 structure):
```yaml
pose0: <TOPIC_FROM_WAVE_0>
pose0_config: [false, false, false,    # x, y, z — do NOT consume (D-04: avoid map→odom cycle)
               false, false, true,     # roll, pitch, YAW ← only this (D-02)
               false, false, false,    # vx, vy, vz
               false, false, false,    # vroll, vpitch, vyaw (D-08 retains yaw_vel on imu0)
               false, false, false]    # ax, ay, az
pose0_differential: false
pose0_relative: false
pose0_queue_size: 10
pose0_rejection_threshold: 3.0
```

Target `yaw_covariance_scale` line for slam_toolbox_params.yaml (OVERRIDE-comment style per file
convention, insert near line 70 after existing Scan Matcher OVERRIDEs):
```yaml
yaw_covariance_scale: <VALUE>     # OVERRIDE: Phase 7 FUSE-01..04 — tunes pose yaw variance toward
                                  # ~0.05 rad² (≈13° std dev) conservative start per D-05. Chosen
                                  # value selected via scripts/yaw-drift-test.sh tuning loop; see
                                  # ekf.yaml FUSE-04 block for full history.
```

FUSE-04 ADR comment block (goes immediately above `pose0:` line in ekf.yaml — per PATTERNS §FUSE-04):
Must record: (a) chosen scale + resulting variance, (b) rejected alternatives with reasoning,
(c) why imu0 yaw-position is false but yaw_vel/roll_vel/pitch_vel stay true (D-08, D-09),
(d) why the numerical override lives in slam_toolbox_params.yaml, not here (RESEARCH §Pitfall 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit config/ekf.yaml (pose0 block + imu0 yaw reconfirm + FUSE-04 ADR comment)</name>
  <read_first>
    - config/ekf.yaml (entire file — existing imu0 block is the in-file template)
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-01-SUMMARY.md §Wave 0 Handoff (topic_name)
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-PATTERNS.md §config/ekf.yaml
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-RESEARCH.md §Code Examples "ekf.yaml delta"
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-CONTEXT.md D-02, D-03, D-04, D-06, D-08, D-09, D-10
  </read_first>
  <files>config/ekf.yaml</files>
  <action>
    Two coordinated edits in ONE file, ONE commit (D-03 simultaneity requirement).

    **Edit A — pose0 block:** Add a new block immediately after the existing `imu0_*` parameters
    (mirrors imu0 shape, per PATTERNS §config/ekf.yaml). Use the topic name from Wave 0 handoff
    (`/pose` or `/slam_toolbox/pose` — whichever live `ros2 topic list` reported). The selection
    vector has exactly ONE true at index 5 (yaw position); every other index is false. Set
    `pose0_differential: false`, `pose0_relative: false`, `pose0_queue_size: 10`,
    `pose0_rejection_threshold: 3.0`. Do NOT add any covariance parameter here — RESEARCH §Pitfall 1
    confirms robot_localization has no such knob; the scale lives in Task 2's slam_toolbox_params.yaml.

    **Edit B — imu0 yaw-position confirmation:** Inspect the existing `imu0_config` selection
    vector. Per PATTERNS discovery, index 5 (6th entry, second row) is ALREADY false in the current
    file. Leave the boolean value as-is. The work here is to UPDATE the inline comment on that row
    to explicitly record the FUSE-01 rationale (e.g. `# roll, pitch, yaw — yaw deaktiviert (FUSE-01:
    avoid correlated input with pose0); yaw_vel below retained per D-08`). Preserve
    `roll_vel/pitch_vel/yaw_vel = true` on the angular-velocity row verbatim (D-08).

    **Edit C — FUSE-04 ADR comment block:** Directly above the `pose0:` line, insert a heredoc-style
    YAML comment block (matching the file's existing German-top + inline-English convention).
    Required content:
    - Header: `# ═══ FUSE-04: pose0 YAW VARIANCE — WHY, WHAT, REJECTED ═══`
    - "WHY pose0 YAW VARIANCE": explain that robot_localization does NOT accept per-sensor
      covariance override; the scale is applied source-side via `yaw_covariance_scale` in
      `config/slam_toolbox_params.yaml`. Point the reader there.
    - "Chosen:": the numerical `yaw_covariance_scale` value (fill in once Task 2 + Task 4 loop
      settles — initial value from Wave 0 handoff, final value after tuning).
    - "Rejected alternatives": (a) aggressive ~0.001 rad² — rejected per D-07 (locks onto SLAM bias);
      (b) consuming native slam_toolbox covariance unexamined — rejected per D-05;
      (c) custom republisher node — rejected per RESEARCH Alternatives Considered (extra
      container + failure mode, zero gain).
    - "IMU rationale": imu0 yaw-position = false (D-03 correlated-input trap); yaw_vel = true
      (D-08: gyro is orthogonal direct measurement; disabling would force rate estimation via
      finite-differencing ~10Hz pose messages per D-09).
    - Reference: `STATE.md §Key Decisions — single-EKF yaw-only pose0`.

    **Critical preservation (do NOT touch):** `world_frame: odom` (D-04 — flipping to map creates
    duplicate map→odom publisher, breaking ROADMAP SC#4). `two_d_mode`, `publish_tf`, `frequency`,
    all other EKF params stay untouched. `imu0_remove_gravitational_acceleration`, `imu0_queue_size`
    stay as-is.

    Grep regression target (used by Task 5 verification):
    - `grep -E '^\s*pose0:\s*/' config/ekf.yaml` must match
    - `grep -q "WHY pose0 YAW VARIANCE" config/ekf.yaml` must match
    - `grep -q "Chosen:" config/ekf.yaml` must match
  </action>
  <verify>
    <automated>python3 -c "import yaml; c=yaml.safe_load(open('config/ekf.yaml'))['ekf_filter_node']['ros__parameters']; p=c['pose0_config']; i=c['imu0_config']; assert sum(p)==1 and p[5]==True, f'pose0_config: {p}'; assert i[5]==False, f'imu0 yaw-position must be false: {i}'; assert i[11]==True, f'imu0 yaw_vel must stay true (D-08): {i}'; assert c['world_frame']=='odom', f'world_frame must stay odom (D-04): {c[\"world_frame\"]}'; print('OK')"</automated>
  </verify>
  <done>
    YAML loads, selection vectors satisfy FUSE-01 + D-02 + D-03 + D-08, world_frame unchanged,
    FUSE-04 ADR comment block present and greppable.
  </done>
  <acceptance_criteria>
    - [ ] `pose0: <TOPIC>` uses the exact topic name from Wave 0 handoff
    - [ ] `pose0_config` sum of true entries == 1, located at index 5 (yaw position)
    - [ ] `pose0_differential: false`, `pose0_relative: false`, `pose0_queue_size: 10`, `pose0_rejection_threshold: 3.0` all present
    - [ ] `imu0_config` index 5 (yaw position) == false; indices 9/10/11 (roll_vel/pitch_vel/yaw_vel) all == true
    - [ ] `world_frame: odom` preserved (D-04)
    - [ ] FUSE-04 ADR block above pose0 contains headers "WHY pose0 YAW VARIANCE", "Chosen:", rejected-alternatives section, and IMU-yaw-retention rationale
    - [ ] File parses as valid YAML
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Edit config/slam_toolbox_params.yaml (yaw_covariance_scale OVERRIDE)</name>
  <read_first>
    - config/slam_toolbox_params.yaml (entire file — existing OVERRIDE comment convention)
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-01-SUMMARY.md §Wave 0 Handoff (native_yaw_cov)
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-PATTERNS.md §config/slam_toolbox_params.yaml
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-RESEARCH.md §Pitfall 1 (why this lives here, not in ekf.yaml)
  </read_first>
  <files>config/slam_toolbox_params.yaml</files>
  <action>
    Add a new line `yaw_covariance_scale: <INITIAL_VALUE>` near the existing Scan Matcher OVERRIDE
    block (after the existing `max_laser_range` OVERRIDE entry, same indentation).

    **Choosing the initial value:** Compute it from Wave 0's `native_yaw_cov` reading to target
    D-05's ~0.05 rad² conservative start.
    - If slam_toolbox publishes native covariance near 0 or 1e-6 (overconfident, most common):
      set `yaw_covariance_scale: 50000.0` — multiplies the tiny native variance up toward ~0.05.
    - If native covariance is already ~0.01 rad²: set `yaw_covariance_scale: 5.0`.
    - If native covariance is already ~0.05 rad² or larger: set `yaw_covariance_scale: 1.0`.

    Use trailing OVERRIDE comment matching file convention (per PATTERNS — same style as
    `transform_publish_period: 0.05    # OVERRIDE: 20 Hz map->odom ...`):

    ```yaml
    yaw_covariance_scale: <VALUE>     # OVERRIDE: Phase 7 FUSE-01..04 — scales pose yaw variance
                                      # toward ~0.05 rad² (≈13° std dev) conservative start per D-05.
                                      # Native covariance[35] from Wave 0: <NATIVE_VALUE>. See
                                      # config/ekf.yaml FUSE-04 block for tuning history + rejected
                                      # alternatives. Final value settled in Task 4 drift-test loop.
    ```

    This initial value will likely be re-tuned in Task 4. That's expected. Do NOT touch any other
    slam_toolbox param — resolution, transform_publish_period, map_update_interval, laser-range
    entries all stay verbatim.
  </action>
  <verify>
    <automated>python3 -c "import yaml; c=yaml.safe_load(open('config/slam_toolbox_params.yaml')); params = c.get('slam_toolbox',{}).get('ros__parameters',{}) if 'slam_toolbox' in c else c.get('/**',{}).get('ros__parameters',{}); assert 'yaw_covariance_scale' in params, 'yaw_covariance_scale missing'; assert isinstance(params['yaw_covariance_scale'], (int, float)), 'must be numeric'; print(f'yaw_covariance_scale = {params[\"yaw_covariance_scale\"]}')"</automated>
  </verify>
  <done>
    YAML loads; `yaw_covariance_scale` is a numeric value; OVERRIDE comment present with reference
    to FUSE-04 block.
  </done>
  <acceptance_criteria>
    - [ ] `yaw_covariance_scale:` key exists in the slam_toolbox params section
    - [ ] Value is a finite positive number derived from Wave 0's native_yaw_cov reading
    - [ ] Trailing OVERRIDE comment matches file convention (inline, references FUSE-04 block in ekf.yaml)
    - [ ] No other slam_toolbox params touched
    - [ ] File parses as valid YAML
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Restart nav + slam containers and verify TF topology</name>
  <read_first>
    - docker-compose.yml (service definitions for `nav` and `slam`)
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-CONTEXT.md D-04 (slam_toolbox sole publisher of map→odom)
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-VALIDATION.md task 07-02-05
  </read_first>
  <files></files>
  <action>
    Over SSH on the mower (ssh pi@10.10.40.23):
    1. `cd ~/MowerBot && git pull` (pull the commits from Tasks 1 + 2 — assumes those were committed
       and pushed from dev box; if executor is working directly on the mower, skip the pull).
    2. Restart both affected containers so config changes load:
       - `docker compose restart slam` (picks up `slam_toolbox_params.yaml` change)
       - `docker compose restart nav` (picks up `ekf.yaml` change)
    3. Wait 5 seconds for both to come up.
    4. Verify EKF is consuming pose0 without errors:
       `docker logs mower-nav --tail 100 2>&1 | grep -iE 'pose0|error|failed|exception' | head -20`
       Expected: at least one log line referencing pose0 subscription; NO `Exception` / `Failed to
       subscribe` / `tf timeout` errors.
    5. Verify TF topology (ROADMAP SC#4 — exactly one publisher of map→odom):
       `docker exec mower-nav timeout 5 ros2 run tf2_ros tf2_echo map odom 2>&1 | head -10`
       Expected: transform is being published, NO "Warning: Could not find transform" errors.
    6. Confirm slam_toolbox is the source (not EKF):
       `docker exec mower-nav ros2 topic echo /tf --once 2>&1 | grep -A2 'frame_id: "map"' | head -6`
       Expected: child_frame_id is `odom`, published at slam_toolbox's rate (~20 Hz per
       `transform_publish_period: 0.05`). If EKF were (incorrectly) publishing map→odom, there would
       be two publishers at different rates.

    If any of these checks fail, DO NOT proceed to Task 4. Report the failure and stop.
  </action>
  <verify>
    <automated>ssh pi@10.10.40.23 'docker exec mower-nav timeout 5 ros2 run tf2_ros tf2_echo map odom 2>&1 | grep -qE "Translation:|At time" && echo OK' | grep -q OK</automated>
  </verify>
  <done>
    Both containers restarted cleanly, no EKF subscription errors in logs, `tf2_echo map odom`
    succeeds within 5 s, slam_toolbox is the sole map→odom publisher.
  </done>
  <acceptance_criteria>
    - [ ] `docker compose restart slam` and `docker compose restart nav` both return exit 0
    - [ ] `docker logs mower-nav` shows pose0 subscription line, no subscription errors
    - [ ] `tf2_echo map odom` returns a transform within 5 s
    - [ ] Only one publisher of `map→odom` (slam_toolbox); no EKF duplicate
    - [ ] Task 4 is safe to proceed (otherwise stop and report)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run FUSE-02 drift-test tuning loop (iterate yaw_covariance_scale until PASS <1°)</name>
  <read_first>
    - scripts/yaw-drift-test.sh (the Wave 0 validator)
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-CONTEXT.md D-06 (tuning loop), D-16 (outdoor stationary)
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-VALIDATION.md §Sampling Rate
  </read_first>
  <files>config/slam_toolbox_params.yaml, config/ekf.yaml</files>
  <action>
    Iterate the tuning loop per D-06. This task may require 2-3 hardware trial rounds.

    Loop body (per trial):
    1. Ensure mower is outdoors, on ground, stationary, motors off, LiDAR seeing features (D-16).
    2. Run `./scripts/yaw-drift-test.sh 60` on the mower. Record the result line.
    3. Decision table:
       - **Δyaw < 0.5° → PASS with margin.** Ship this `yaw_covariance_scale` value. Update the
         "Chosen:" line in the FUSE-04 ADR comment block in `config/ekf.yaml` to record the final
         scale + the resulting effective variance estimate. Also update the inline OVERRIDE comment
         in `config/slam_toolbox_params.yaml` to list this trial's result. Proceed to Task 5.
       - **0.5° ≤ Δyaw < 1.0° → marginal PASS.** Per D-16: re-run once more. If second run is also
         in [0.5°, 1.0°], ship it but note the marginal pass in the FUSE-04 comment. If second run
         swings to FAIL, treat as FAIL and re-tune.
       - **Δyaw ≥ 1.0° → FAIL.** Tighten `yaw_covariance_scale` (smaller number = more trust in
         SLAM → faster convergence, less drift accumulation on imu yaw_vel). Reduce by ~3-5× (e.g.
         50000 → 15000 → 5000 → 1500). Re-commit `slam_toolbox_params.yaml`, `docker compose
         restart slam`, wait 5 s, re-run the test. Cap iterations at 5; if still failing, STOP and
         report (may indicate the D-07 floor has been hit — don't go below ~0.001 rad² effective).

    4. For every trial, append to FUSE-04 ADR comment block in `config/ekf.yaml` under
       "Rejected alternatives" a one-line record: `# - yaw_covariance_scale=<VAL> → Δyaw=<X.XX°>
       over 60s (rejected: drift too high)`.

    After convergence, update both config files in a single final commit titled something like
    `tune(phase-07): yaw_covariance_scale=<FINAL_VALUE> — Δyaw=<X.XX°> PASS`.
  </action>
  <verify>
    <automated>ssh pi@10.10.40.23 'cd ~/MowerBot && ./scripts/yaw-drift-test.sh 60'</automated>
  </verify>
  <done>
    `./scripts/yaw-drift-test.sh 60` exits 0 on live hardware. Final `yaw_covariance_scale` is
    recorded in both `slam_toolbox_params.yaml` (OVERRIDE comment) and `ekf.yaml` (FUSE-04 "Chosen:"
    line). At least one rejected alternative recorded in FUSE-04 block.
  </done>
  <acceptance_criteria>
    - [ ] Final `./scripts/yaw-drift-test.sh 60` exits 0 (PASS) on live mower
    - [ ] Final Δyaw is <1.0° (hard), target <0.5° (preferred)
    - [ ] FUSE-04 "Chosen:" line in `config/ekf.yaml` records the final numeric value
    - [ ] FUSE-04 "Rejected alternatives" lists each scale value tried with its drift result
    - [ ] `config/slam_toolbox_params.yaml` OVERRIDE comment lists the final trial result
    - [ ] If marginal (0.5°–1.0°), second trial confirms; note recorded in FUSE-04 comment
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human-verify end-to-end FUSE-01/02/04 landed cleanly on hardware</name>
  <read_first>
    - .planning/phases/07-slam-pose-ekf-yaw-fusion/07-VALIDATION.md §Validation Sign-Off
  </read_first>
  <what-built>
    - `config/ekf.yaml`: pose0 block + imu0 yaw-position confirmed false + FUSE-04 ADR comment
    - `config/slam_toolbox_params.yaml`: `yaw_covariance_scale` with OVERRIDE comment
    - Both containers restarted, TF topology verified, FUSE-02 drift test PASSing
  </what-built>
  <action>Operator executes the verification steps defined in <how-to-verify> below on live hardware and reports results. No Claude-side implementation beyond presenting the checklist.</action>
  <how-to-verify>
    Operator runs these five checks on the mower and confirms all pass:

    1. **FUSE-01 static checks (local dev box or over SSH):**
       ```bash
       grep -E '^\s*pose0:\s*/' config/ekf.yaml
       python3 -c "import yaml; c=yaml.safe_load(open('config/ekf.yaml'))['ekf_filter_node']['ros__parameters']; assert sum(c['pose0_config'])==1 and c['pose0_config'][5]; assert not c['imu0_config'][5]; assert c['imu0_config'][11]; print('FUSE-01 OK')"
       ```

    2. **FUSE-04 ADR comment greppable:**
       ```bash
       grep -q "WHY pose0 YAW VARIANCE" config/ekf.yaml && grep -q "Chosen:" config/ekf.yaml && echo "FUSE-04 OK"
       ```

    3. **FUSE-02 drift test PASS (mower outdoor, stationary, motors off, LiDAR seeing features):**
       ```bash
       ssh pi@10.10.40.23 'cd ~/MowerBot && ./scripts/yaw-drift-test.sh 60'
       echo "exit=$?"
       ```
       Expected: `Δyaw = X.XX° over 60s — PASS` and `exit=0`. Paste the line.

    4. **TF topology — slam_toolbox sole publisher:**
       ```bash
       ssh pi@10.10.40.23 'docker exec mower-nav timeout 5 ros2 run tf2_ros tf2_echo map odom 2>&1 | head -5'
       ```
       Expected: active transform; no warnings.

    5. **No EKF subscription errors in logs since last restart:**
       ```bash
       ssh pi@10.10.40.23 'docker logs mower-nav --since 5m 2>&1 | grep -iE "error|fail|exception" | head -10'
       ```
       Expected: empty, or only benign startup warnings.

    Paste all five outputs and type `approved` to unblock Wave 2 (web badge).
  </how-to-verify>
  <resume-signal>
    Paste all five verification outputs + drift test PASS line, then "approved". Or describe
    blockers (e.g., drift still failing, container crash, TF missing).
  </resume-signal>
  <acceptance_criteria>
    - [ ] FUSE-01 static checks all pass (pose0 present, pose0_config[5]=true, imu0_config[5]=false, imu0_config[11]=true)
    - [ ] FUSE-04 grep targets match
    - [ ] FUSE-02 drift test prints PASS line, exit 0 on live hardware
    - [ ] `tf2_echo map odom` shows active transform, slam_toolbox is sole publisher
    - [ ] `docker logs mower-nav` clean of errors for 5 min window post-restart
    - [ ] Operator has physically seen drift test output on outdoor/stationary mower
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/ekf.yaml → ekf_filter_node | YAML params loaded at node startup inside nav container; trusted-LAN config artifact, no runtime re-load path |
| config/slam_toolbox_params.yaml → slam_toolbox | Same pattern; loaded at slam container startup |
| /pose topic → ekf_filter_node | Untrusted-within-boundary: NaN or malformed pose field. ekf built-in validators handle outliers via pose0_rejection_threshold (Mahalanobis 3.0) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | T (Tampering) | /pose with wild/malformed yaw from SLAM scan-match failure | mitigate | `pose0_rejection_threshold: 3.0` (Mahalanobis) drops outliers; FUSE-02 drift test proves end-to-end filter stability under stationary conditions |
| T-07-05 | I (Info disclosure) | ekf.yaml FUSE-04 ADR comment block | accept | Documentation reveals algorithmic decisions; trusted-LAN device; no secrets |
| T-07-06 | D (Denial of service) | /pose topic stops publishing (slam_toolbox crash) | accept | Per D-13: EKF coasts on imu yaw_vel; badge turns red in Wave 2; operator acts. No auto-fallback (D-14 deferred to future safety milestone) |
| T-07-07 | E (Elevation) | world_frame accidentally flipped odom→map | mitigate | Task 1 verify step asserts `world_frame == 'odom'`; Task 3 TF check confirms slam_toolbox is sole map→odom publisher |
</threat_model>

<verification>
- `config/ekf.yaml` YAML loads; pose0_config has exactly one true at index 5; imu0_config[5]=false, imu0_config[11]=true
- FUSE-04 ADR block contains "WHY pose0 YAW VARIANCE", "Chosen:", rejected-alternatives section
- `config/slam_toolbox_params.yaml` YAML loads; `yaw_covariance_scale` present with OVERRIDE comment
- `./scripts/yaw-drift-test.sh 60` exits 0 on live mower
- `tf2_echo map odom` succeeds; slam_toolbox is sole map→odom publisher
- Nav container logs clean of errors since config-reload restart
</verification>

<success_criteria>
1. FUSE-01 landed in a single commit: pose0 block added AND imu0 yaw-position locked false AND yaw_vel retained
2. FUSE-02 PASSes with margin (target <0.5°, hard <1.0°) on live outdoor stationary test
3. FUSE-04 ADR comment block present in `config/ekf.yaml` with chosen value + rejected alternatives + IMU rationale
4. slam_toolbox remains sole publisher of map→odom (ROADMAP SC#4)
5. Wave 2 (web badge) has a known-healthy `/pose` topic to subscribe to
</success_criteria>

<output>
After completion, create `.planning/phases/07-slam-pose-ekf-yaw-fusion/07-02-SUMMARY.md` recording:
- Final `yaw_covariance_scale` value + all rejected trials with drift readings
- Final FUSE-02 drift-test PASS line
- TF topology confirmation (slam_toolbox sole publisher)
- Commit hash(es) that landed the simultaneity commit
</output>
