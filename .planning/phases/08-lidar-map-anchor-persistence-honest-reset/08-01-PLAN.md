---
phase: 08-lidar-map-anchor-persistence-honest-reset
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - config/empty.posegraph
  - config/empty.data
  - web/__tests__/map-epoch.test.mjs
  - web/__tests__/map-frame-pose.test.mjs
  - web/__tests__/map-store-rehydrate.test.mjs
  - web/__tests__/map-reset-route.test.mjs
autonomous: false
requirements:
  - MAP-01
  - MAP-03
  - MAP-04
user_setup:
  - service: mower-hardware-ssh
    why: "Wave 0 Task 1 requires running `ros2 service call /slam_toolbox/serialize_map` on the running Pi to generate config/empty.posegraph + config/empty.data. This is a one-time fixture generation that cannot be faked — the .posegraph binary format is produced by the live slam_toolbox container. User has authorized SSH access to 10.10.40.23 (see MEMORY.md/ssh-pi-access.md)."
    env_vars: []
    dashboard_config:
      - task: "Confirm mower Pi powered on and reachable at 10.10.40.23"
        location: "Physical mower + network"
      - task: "Confirm `docker compose ps` shows mower-slam running and /scan publishing (so slam has something to map)"
        location: "SSH into mower Pi"

must_haves:
  truths:
    - "config/empty.posegraph and config/empty.data exist, committed under /config, generated by slam_toolbox/serialize_map on a freshly-restarted slam container (so the posegraph contains no submaps, exactly what Option B deserialize needs to wipe state)"
    - "Five RED-state test files exist and run via `npm run test` — they fail until Wave 1..3 lands the source modules they import (node:test's dynamic-import pattern: ENOENT throws become the first RED signal)"
    - "server-upgrade.test.mjs (Phase 6 regression gate) stays GREEN — no edits to server.mjs are introduced by this plan"
    - "Each test file covers its MAP-XX requirement per §Validation Architecture: epoch atomic write, composite pose math, rehydrate+cleanup+quota, 3 response shapes of /api/map/reset"
    - "map-reset-route.test.mjs encodes two regression gates that Plan 02 must satisfy: (a) the handler passes minStampMs to waitForNextMap and drops TL-latched /map messages with older stamps (stale-latch filter, BLOCKER #2); (b) the handler drains the waitForNextMap promise on the slamDeserializeEmpty-failure path (Plan 02 step 7) so no WS/topic leaks when the service call fails (BLOCKER #1). Note: the D-15 early-return path is leak-free by construction in the post-revision architecture — waitForNextMap is started AFTER bumpEpoch resolves, so there is nothing to drain when bumpEpoch rejects."
  artifacts:
    - path: "config/empty.posegraph"
      provides: "Binary slam_toolbox pose-graph fixture representing an empty map; consumed by Wave 1 slam-reset helper via /slam_toolbox/deserialize_map"
    - path: "config/empty.data"
      provides: "Companion data file to empty.posegraph (slam_toolbox serializes as a pair); mandatory — deserialize_map fails if absent"
    - path: "web/__tests__/map-epoch.test.mjs"
      provides: "Unit tests: readEpoch default-on-missing, bumpEpoch monotonic increment, writeEpochAtomic temp-then-rename atomicity"
      min_lines: 40
    - path: "web/__tests__/map-frame-pose.test.mjs"
      provides: "Unit tests for computeMapFramePose: zero-delta case (mapFramePose==slam), rotated-frame case (odom delta composed via slam.yaw - anchorOdom.yaw rotation), null-slam fallback"
      min_lines: 40
    - path: "web/__tests__/map-store-rehydrate.test.mjs"
      provides: "Store tests with stubbed fetch + stubbed localStorage: rehydrate happy path, setEpoch cleans stale keys, QuotaExceededError → persistenceDisabled: true"
      min_lines: 60
    - path: "web/__tests__/map-reset-route.test.mjs"
      provides: "Integration tests for handleReset (6 cases): happy {ok:true}, {ok:false,stage:service}, {ok:false,stage:mapTimeout}, epoch-write-failure-before-slam-call ordering (D-15), stale-latch filter via minStampMs (BLOCKER #2), clean unsubscribe on slamDeserializeEmpty-failure path (Plan 02 step 7, BLOCKER #1)"
      min_lines: 120
  key_links:
    - from: "config/empty.posegraph"
      to: "web/lib/server/slam-reset.mjs (Plan 02)"
      via: "/slam_toolbox/deserialize_map service call with filename: '/config/empty'"
      pattern: "filename.*/config/empty"
    - from: "web/__tests__/*.test.mjs"
      to: "web/lib/server/* and web/lib/store/* (Plans 02-03)"
      via: "dynamic import at test-time — imports resolve RED until modules ship, GREEN after"
      pattern: "await import\\(\"\\.\\./(lib|app)/"
---

<objective>
Wave 0: seed the Option-B reset payload (a fresh empty slam_toolbox pose-graph, generated once on real hardware) and scaffold the five RED-state test files that gate every subsequent wave. Generating the empty posegraph here — not later — is what preserves Phase 6's `docker.sock:ro` security boundary (Option A would have forced `:rw` + dockerode container.restart; per post-research D-13a we explicitly reject that). The five test files establish Nyquist-compliant feedback loops: every subsequent code task in Plans 02/03/04 has an automated verify that re-runs one of these suites in < 2 s.

Purpose:
- Produce the binary fixture that makes Option-B reset possible at all (`empty.posegraph` + `empty.data` cannot be handwritten — only `serialize_map` against a running slam container generates them correctly)
- Lock the test-first contract for Waves 1-3: every module landed later must pass the tests written here
- Confirm the Phase 6 regression gate (single-upgrade-handler) is still green BEFORE any Phase 8 code lands, so a later breakage is unambiguously attributable

Output:
- 2 config fixtures + 4 test scaffolds (13 test cases total: 4 + 3 + 3 + 6 — the 6 reset-route cases include stale-latch + unsubscribe regression gates per BLOCKERS #1/#2) + 1 regression-gate re-confirmation
- All 4 new test files in RED (ENOENT on import of not-yet-existing modules)
- server-upgrade.test.mjs in GREEN (unchanged)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-CONTEXT.md
@.planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-RESEARCH.md
@.planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-PATTERNS.md
@.planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-VALIDATION.md
@.planning/phases/06-webui-container-logs-view/06-CONTEXT.md
@web/__tests__/since-preset.test.mjs
@web/__tests__/docker-adapter.test.mjs
@web/__tests__/server-upgrade.test.mjs

<interfaces>
<!-- Contracts the RED tests must import against. Do NOT implement — Plans 02/03 do that.
     These are the module shapes Wave 0 tests MUST encode as expectations. -->

From web/lib/server/map-epoch.mjs (to be created in Plan 02):
```javascript
export async function readEpoch(): Promise<{ epoch: number; resetAt: string }>
export async function writeEpochAtomic(data: { epoch: number; resetAt: string }): Promise<void>
export async function bumpEpoch(): Promise<{ epoch: number; resetAt: string }>
```

From web/lib/utils/map-frame-pose.ts (to be created in Plan 03):
```typescript
export interface SlamAnchor {
  x: number; y: number; yaw: number;
  anchorOdom: { x: number; y: number; yaw: number } | null;
}
export interface OdomSnap { posX: number; posY: number; yawRad: number; }
export function computeMapFramePose(
  slam: SlamAnchor | null,
  odom: OdomSnap,
): { x: number; y: number; yaw: number } | null
```

From web/lib/store/map-store.ts (extended in Plan 03):
```typescript
// New fields on state:
//   epoch: number | null
//   persistenceDisabled: boolean
// New actions:
//   setEpoch(n: number): void        // also drops mowerbot.map.epoch.<k> where k < n
//   rehydrate(): Promise<void>        // GET /api/map/epoch → load localStorage → set latest + isStale:true
// updateMap() extended: writes JSON.stringify({...m, data: Array.from(m.data)}) to
//   mowerbot.map.epoch.<currentEpoch>, swallows QuotaExceededError → persistenceDisabled = true
```

From web/app/api/map/reset/route.ts (to be created in Plan 02):
```typescript
export async function POST(): Promise<NextResponse>
// Response shapes:
//   { ok: true, epoch, mapReceived: true, elapsedMs }           // HTTP 200
//   { ok: false, stage: "service", error }                      // HTTP 500 or 502
//   { ok: false, stage: "mapTimeout", epoch, error }            // HTTP 200 — epoch IS bumped
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Generate empty.posegraph on live hardware (USER-REQUIRED SSH)</name>
  <files>config/empty.posegraph, config/empty.data</files>
  <what-built>Plan 08-01 needs an empty slam_toolbox pose-graph fixture committed to config/. The .posegraph + .data files are binary artifacts produced by running `/slam_toolbox/serialize_map` against a freshly-restarted slam container (no scans yet → empty state). Claude cannot generate these locally — they must come from the Pi.</what-built>
  <read_first>
    - .planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-RESEARCH.md §Blocking Finding (Option B procedure, lines 148-152)
    - .planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-CONTEXT.md §D-13a (the post-research decision locking Option B)
    - docker-compose.yml lines 168-184 (mower-slam service, mounts ./config:/config:ro)
    - MEMORY.md/ssh-pi-access.md (user authorized password-based SSH to 10.10.40.23)
  </read_first>
  <how-to-verify>
    1. From the developer laptop, SSH into the mower:
       `ssh danny@10.10.40.23` (password-based, see ssh-pi-access.md)
    2. Navigate to the MowerBot repo on the Pi:
       `cd ~/MowerBot` (or wherever docker-compose.yml lives)
    3. Restart slam to guarantee an empty internal state, then wait for it to be subscribed:
       `docker compose restart slam && sleep 5`
    4. Confirm slam is alive and the serialize service is listed:
       `docker exec mower-slam bash -c "source /opt/ros/humble/setup.bash && ros2 service list | grep slam_toolbox/serialize_map"`
       Expected: prints `/slam_toolbox/serialize_map`
    5. Call serialize_map, telling slam to write to /config/empty (mounted from host ./config):
       `docker exec mower-slam bash -c "source /opt/ros/humble/setup.bash && ros2 service call /slam_toolbox/serialize_map slam_toolbox/srv/SerializePoseGraph '{filename: \"/config/empty\"}'"`
       Expected response includes `result: 0` (success)
    6. Verify the two files landed on the host (via the :ro bind-mount, slam_toolbox actually writes back to the host since the mount is read-write from slam's perspective for this path — wait, it IS :ro per ros-common. **If the bind is read-only, the write will fail.** Fallback: write to /tmp first, then `docker cp mower-slam:/tmp/empty.posegraph ./config/` + same for .data):

       FIRST try: `ls -la ~/MowerBot/config/empty.posegraph ~/MowerBot/config/empty.data`
       If missing (because ./config is :ro), use the docker-cp fallback:
         `docker exec mower-slam bash -c "source /opt/ros/humble/setup.bash && ros2 service call /slam_toolbox/serialize_map slam_toolbox/srv/SerializePoseGraph '{filename: \"/tmp/empty\"}'"`
         `docker cp mower-slam:/tmp/empty.posegraph ~/MowerBot/config/empty.posegraph`
         `docker cp mower-slam:/tmp/empty.data ~/MowerBot/config/empty.data`

    7. SCP the two files back to the development Mac:
       `scp danny@10.10.40.23:~/MowerBot/config/empty.posegraph /Users/danny.smolinsky/jam-dev/MowerBot/config/`
       `scp danny@10.10.40.23:~/MowerBot/config/empty.data /Users/danny.smolinsky/jam-dev/MowerBot/config/`
    8. Verify on the Mac:
       `ls -la /Users/danny.smolinsky/jam-dev/MowerBot/config/empty.posegraph /Users/danny.smolinsky/jam-dev/MowerBot/config/empty.data`
       Expected: both files present, non-zero size (posegraph is typically 1-10 KB for an empty graph, data is typically a few bytes of header)
       `file config/empty.posegraph` → should report as binary data, not ASCII
    9. Smoke-test the fixture by calling deserialize_map with it (confirms the pair is valid before committing):
       `docker exec mower-slam bash -c "source /opt/ros/humble/setup.bash && ros2 service call /slam_toolbox/deserialize_map slam_toolbox/srv/DeserializePoseGraph '{filename: \"/config/empty\", match_type: 1, initial_pose: {x: 0.0, y: 0.0, theta: 0.0}}'"`
       Expected: `result: 0`, and `docker compose logs --tail=50 slam` shows no errors about missing or malformed files.
    10. Report back to Claude: "posegraph fixture generated, both files committed locally, deserialize round-trip OK"
  </how-to-verify>
  <acceptance_criteria>
    - /Users/danny.smolinsky/jam-dev/MowerBot/config/empty.posegraph exists, non-zero size, identified as binary by `file`
    - /Users/danny.smolinsky/jam-dev/MowerBot/config/empty.data exists, non-zero size
    - Deserialize smoke-call on the Pi returned success (result: 0)
    - Both files added to git via Task 5 (final commit task)
  </acceptance_criteria>
  <action>USER-REQUIRED: follow the `<how-to-verify>` steps above — SSH to the mower, call /slam_toolbox/serialize_map, SCP the resulting empty.posegraph + empty.data back to this repo's config/ directory. Claude cannot execute this autonomously because the binary .posegraph format can only be produced by a live slam_toolbox container on real hardware. Report back with observed file sizes + the deserialize smoke-test result from step 9.</action>
  <verify>
    <automated>test -s /Users/danny.smolinsky/jam-dev/MowerBot/config/empty.posegraph && test -s /Users/danny.smolinsky/jam-dev/MowerBot/config/empty.data && file /Users/danny.smolinsky/jam-dev/MowerBot/config/empty.posegraph | grep -v ASCII</automated>
  </verify>
  <done>Both config/empty.posegraph and config/empty.data exist on the Mac, non-zero size, binary content; deserialize_map smoke call on the Pi returned result:0.</done>
  <resume-signal>Type "fixtures generated" or paste `ls -la config/empty.*` output</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold map-epoch.test.mjs (MAP-03, MAP-04 atomic write coverage)</name>
  <files>web/__tests__/map-epoch.test.mjs</files>
  <read_first>
    - web/__tests__/since-preset.test.mjs (template — same framework, same shape: dynamic import + node:test + assert/strict)
    - .planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-PATTERNS.md §map-epoch.test.mjs (lines 711-736)
    - .planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-RESEARCH.md §Pattern 2 (atomic write pattern, lines 384-413) and §Epoch File I/O (lines 765-772)
  </read_first>
  <action>
Create web/__tests__/map-epoch.test.mjs with three tests that RED-import `web/lib/server/map-epoch.mjs` (which does not yet exist — Plan 02 creates it). Use node:test + assert/strict + dynamic import, matching since-preset.test.mjs verbatim. Use a per-test tmpdir from `node:os` + `node:fs` so tests never touch the real data/map-epoch.json.

**Critical:** the map-epoch module uses a constant path `path.join(process.cwd(), "data", "map-epoch.json")` — tests cannot override that via function args (the module closes over EPOCH_FILE). Two options: (a) run the test with cwd set to a tmpdir via `process.chdir(tmpdir)` in `before()`/`after()`, OR (b) the tests read the file from the real data/ location with a known unique key and clean up after. **Recommend option (a)** — it keeps map-epoch.mjs simple and matches how docker-adapter.test.mjs stubs its globals. Use `node:test`'s `before`/`after` hooks to manage process.chdir().

Test cases (one `test()` call each):
  1. "readEpoch returns {epoch: 0, resetAt: <ISO>} when file missing" — start with empty tmpdir/data/, call readEpoch(), assert epoch === 0 and Date.parse(resetAt) is finite
  2. "writeEpochAtomic + readEpoch round-trips a value" — write {epoch: 7, resetAt: "2026-04-17T..."}, re-read, assert deep equal
  3. "bumpEpoch increments monotonically from any starting value" — seed file with {epoch: 4, ...}, call bumpEpoch() three times, assert results are epoch=5, 6, 7 with strictly increasing resetAt timestamps
  4. "writeEpochAtomic leaves no half-written file on reader races" — after writeEpochAtomic resolves, assert the file exists and JSON.parse succeeds (guards against skipping the rename step; NOT a true torn-read race test since those are hard in node:test, but a post-condition check is sufficient)

Import path: `await import("../lib/server/map-epoch.mjs")`. Use template literal path for tmpdir: `await fs.mkdtemp(path.join(os.tmpdir(), "mowbot-epoch-"))`. The module will NOT import until Plan 02 lands — this is the intended RED state (test fails with ENOENT at import time).
  </action>
  <behavior>
    - readEpoch() on missing file → {epoch: 0, resetAt: <valid ISO>}
    - writeEpochAtomic({epoch: 7, resetAt: "..."}) then readEpoch() → same object (deep equal)
    - bumpEpoch() called 3 times from {epoch:4} → returns {epoch:5}, {epoch:6}, {epoch:7} in order
    - After writeEpochAtomic resolves, fs.readFile + JSON.parse succeeds with no partial data
  </behavior>
  <verify>
    <automated>cd /Users/danny.smolinsky/jam-dev/MowerBot/web && npm run test -- __tests__/map-epoch.test.mjs 2>&1 | grep -E "(RED|not ok|ENOENT|Cannot find module).*map-epoch"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at web/__tests__/map-epoch.test.mjs
    - At least 4 `test(` calls present
    - Dynamic import targets `../lib/server/map-epoch.mjs`
    - Running the suite RIGHT NOW produces a clear ENOENT / "Cannot find module" failure (RED state confirmed — proves the test is wired to the not-yet-shipped module)
    - File structure matches since-preset.test.mjs conventions
  </acceptance_criteria>
  <done>web/__tests__/map-epoch.test.mjs is RED against the not-yet-existing module; will go GREEN when Plan 02 creates map-epoch.mjs.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold map-frame-pose.test.mjs + map-store-rehydrate.test.mjs (MAP-01 + MAP-03 coverage)</name>
  <files>web/__tests__/map-frame-pose.test.mjs, web/__tests__/map-store-rehydrate.test.mjs</files>
  <read_first>
    - web/__tests__/demux.test.mjs (pattern: pure-function fixture test with arrange-act-assert)
    - web/__tests__/docker-adapter.test.mjs (pattern: dynamic-import + stubbed globals — specifically how it stubs `globalThis.fetch` or module-level singletons)
    - .planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-PATTERNS.md §map-frame-pose.test.mjs (lines 740-762) and §map-store-rehydrate.test.mjs (lines 766-791)
    - .planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-RESEARCH.md §Composite Pose Math (lines 556-623) and §localStorage Quota-Safe setItem (lines 702-762)
  </read_first>
  <action>
Create TWO test files. Each RED-imports its target module via dynamic `await import(...)`.

**File A — web/__tests__/map-frame-pose.test.mjs** (pure math, 3-4 tests):
Import: `await import("../lib/utils/map-frame-pose.js")` (Plan 03 creates this as a `.ts` file; Next.js/tsx transpiles at dev/test time — verify import works by running under `web/package.json`'s `"test": "node --test __tests__/"` which runs against the TS source via the project's existing Node test config. If Node cannot import .ts directly, use the plain-JS analog path `../lib/utils/map-frame-pose.js`; Plan 03 should compile/export accordingly. **Recommend:** Plan 03 creates map-frame-pose as a `.mjs` server utility OR as a `.ts` with a co-located `.js` entry. Safest for Wave 0: write the test assuming a `.mjs` file at `web/lib/utils/map-frame-pose.mjs` — fewer moving parts at test time.)

Tests:
  1. "computeMapFramePose returns null when slam anchor is null" — input (null, odom) → null output
  2. "returns slam pose unchanged when odom == anchorOdom (zero delta)" — slam at {x:1, y:2, yaw:0.5, anchorOdom:{x:5, y:5, yaw:0.1}}, odom at {posX:5, posY:5, yawRad:0.1} → {x:1, y:2, yaw:0.5} exactly
  3. "rotates odom delta into map frame via (slam.yaw - anchorOdom.yaw)" — slam yaw 90°=π/2, anchorOdom yaw 0, odom +1m east of anchor (posX=6, posY=5 with anchor at (5,5)) → mapFramePose should land at (slam.x + 0, slam.y + 1) within ±1e-6 (east-in-odom rotates to north-in-map when frame offset is 90° CCW)
  4. "composes yaw additively when rotation is zero" — slam yaw 0, anchorOdom yaw 0, odom yawRad = 0.3 → mapFramePose.yaw === 0.3

Use `assert.ok(Math.abs(actual - expected) < 1e-6, ...)` for float comparisons, since `assert.equal` is strict-equal.

**File B — web/__tests__/map-store-rehydrate.test.mjs** (store with stubbed browser globals, 3 tests):
This needs `globalThis.localStorage` + `globalThis.fetch` stubs because map-store.ts is a client module that will reference them. Use a simple in-memory Map-backed shim installed in `before()` and torn down in `after()`. Do NOT install jsdom — keep it dependency-free; the store only touches `localStorage.setItem/getItem/removeItem/key/length`.

Tests:
  1. "rehydrate() fetches /api/map/epoch and loads matching localStorage key" — stub fetch → {epoch: 7, resetAt: "..."}, seed localStorage['mowerbot.map.epoch.7'] = JSON.stringify({info:{width:2,height:2,resolution:0.025,origin:{position:{x:0,y:0}}}, data:[0,0,0,0], header:{}}). Call store.rehydrate(). Assert: store.latest !== null, store.latest.data is an Int8Array, store.epoch === 7.
  2. "setEpoch(N) drops localStorage keys with suffix < N" — seed mowerbot.map.epoch.3 / .4 / .5 / .7 / .10. Call setEpoch(7). Assert: .3, .4, .5 removed; .7 and .10 remain. (.10 has suffix 10 which is >= 7, so it stays — this also tests that the test uses integer compare, not string compare.)
  3. "safeSetItem flips persistenceDisabled on QuotaExceededError" — stub localStorage.setItem to throw `Object.assign(new Error(), {name: "QuotaExceededError", code: 22})` (simulate DOMException since it's not available without jsdom — the store check must be loose enough to recognize by name+code). Call store.updateMap({...mock grid...}). Assert: store.persistenceDisabled === true, store.latest was still set (in-memory update should still happen; persistence just drops).

Import: `await import("../lib/store/map-store.ts")`. Since map-store.ts currently imports `"zustand"` which is a browser/ESM package, Node test may need `--experimental-vm-modules` — check by running the existing docker-adapter.test.mjs first to see if zustand-adjacent tests work in this repo. **If zustand fails to resolve under node:test:** fall back to extracting the pure helpers (safeSetItem, isQuotaExceededError, cleanupOldEpochKeys) into `web/lib/store/map-store-helpers.mjs` and test those directly; the Zustand-wrapped store becomes integration-only. Note this as a design constraint to Plan 03.
  </action>
  <behavior>
    map-frame-pose.test.mjs:
      - Null slam → null output
      - Zero odom delta → mapFramePose == slam
      - 90° frame offset → odom east becomes map north (±1e-6)
      - Yaw composes additively when anchorOdom.yaw == slam.yaw
    map-store-rehydrate.test.mjs:
      - rehydrate stubs fetch + localStorage, loads matching key, reconstructs Int8Array
      - setEpoch(N) removes all mowerbot.map.epoch.<k> with k < N, preserves k >= N
      - QuotaExceededError during setItem → persistenceDisabled: true, but in-memory state still updates
  </behavior>
  <verify>
    <automated>cd /Users/danny.smolinsky/jam-dev/MowerBot/web && npm run test -- __tests__/map-frame-pose.test.mjs __tests__/map-store-rehydrate.test.mjs 2>&1 | grep -E "(not ok|ENOENT|Cannot find)"</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist
    - Each has at least 3 `test(` calls covering the behaviors listed
    - Both files RED at Wave 0 with a clear "module not found" or "ENOENT" failure (proves test-to-module wiring)
    - Float tolerance uses ±1e-6 for map-frame-pose
    - map-store-rehydrate installs localStorage + fetch shims in before()/after() — no leaked globals
  </acceptance_criteria>
  <done>Both test scaffolds are RED against not-yet-shipped modules; will go GREEN as Plan 03 lands.</done>
</task>

<task type="auto">
  <name>Task 4: Scaffold map-reset-route.test.mjs (MAP-04 all three response shapes + D-15 ordering)</name>
  <files>web/__tests__/map-reset-route.test.mjs</files>
  <read_first>
    - web/__tests__/docker-adapter.test.mjs (pattern: mock-boundary integration test with dynamic imports)
    - .planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-PATTERNS.md §map-reset-route.test.mjs (lines 794-816)
    - .planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-RESEARCH.md §Example 2 (lines 910-965, the route skeleton) and §Pattern 4 (one-shot subscribe timeout, lines 466-494)
    - .planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-CONTEXT.md §D-13 (three response shapes), D-15 (ordering: file write BEFORE service call), D-17 (mapTimeout still bumps epoch)
  </read_first>
  <action>
Create web/__tests__/map-reset-route.test.mjs with 6 integration tests that RED-import `../lib/server/reset-handler.mjs`. Stub the handler's dependencies via node's module-mocking (use `node:test`'s `mock.module()` API, available in Node 22+) OR by providing fake implementations through env-var indirection. Since `node:test` mock.module was experimental in Node 20 and stabilizing in 22 (which this project uses per docker-compose.yml NODE version), use `mock.module()`.

Tests:
  1. "POST happy path returns {ok:true, epoch, mapReceived:true, elapsedMs}" — stub map-epoch.mjs::bumpEpoch → {epoch: 7, resetAt: "..."}, stub slam-reset.mjs::slamDeserializeEmpty → Promise.resolve({result: 0}), stub slam-reset.mjs::waitForNextMap → mock OccupancyGrid with a FRESH header.stamp (see test #5 for stamp semantics). Call handleReset(). Assert: response.status === 200, body.ok === true, body.epoch === 7, body.mapReceived === true, typeof body.elapsedMs === "number".

  2. "service-stage failure → {ok:false, stage:'service'} HTTP 502" — stub bumpEpoch → {epoch: 7}, stub slamDeserializeEmpty → Promise.reject(new Error("rosbridge connect timeout")). Call handleReset(). Assert: response.status === 502, body.ok === false, body.stage === "service", body.error contains "rosbridge" or "connect timeout".

  3. "mapTimeout stage → {ok:false, stage:'mapTimeout', epoch: 7} HTTP 200, epoch bumped" — stub bumpEpoch → {epoch: 7}, stub slamDeserializeEmpty → Promise.resolve, stub waitForNextMap → Promise.resolve(null). Call handleReset(). Assert: response.status === 200, body.ok === false, body.stage === "mapTimeout", body.epoch === 7, body.error mentions "3000ms" or "no fresh".

  4. "D-15 ordering: epoch write failure returns {ok:false, stage:'service'} BEFORE invoking slam" — stub bumpEpoch → Promise.reject(new Error("EACCES data/map-epoch.json")). Track whether slamDeserializeEmpty was called via a `let called = false;` mock. Call handleReset(). Assert: response.ok === false, response.stage === "service", body.error contains "EACCES", AND called === false (critical: D-15 mandates the file write happens first; on its failure, slam is untouched).

  5. **"stale-latch filter: waitForNextMap rejects pre-reset TL-latched /map" (BLOCKER #2 regression gate)** — The handler must pass a `minStampMs` to waitForNextMap equal to the clock time RIGHT BEFORE slamDeserializeEmpty is invoked (not before bumpEpoch — we want to filter pre-service-call messages, not pre-epoch-bump). Test mechanics: capture `minStampMs` received by the stubbed waitForNextMap via a spy. Then synthesize two OccupancyGrid messages with `header.stamp` as ROS 2's `builtin_interfaces/Time` shape `{sec: number, nanosec: number}` (ROS 2 field name — NOT `nsec`; that was ROS 1). Helper: `const stampFromMs = (ms) => ({ sec: Math.floor(ms / 1000), nanosec: Math.floor((ms % 1000) * 1e6) });`
     - stale: `stamp_ms_stale = Date.now() - 5000` — a stamp clearly predating the service call (the TL-latched previous map)
     - fresh: `stamp_ms_fresh = Date.now() + 1` — a stamp just after (the post-reset publish)
     The real `waitForNextMap` (to be implemented in Plan 02) must drop the stale and accept the fresh. In the test we exercise this by providing a spy `waitForNextMap` that receives `(timeoutMs, minStampMs)`, synthesizes the two messages, and runs the handler's filter predicate (`msgStampMs >= minStampMs`) against each. Assertions:
       (a) The spy was invoked with a `minStampMs` value captured RIGHT BEFORE slamDeserializeEmpty — not before bumpEpoch. Assert `spy.minStampMs >= t_afterBumpEpoch` and `spy.minStampMs <= t_afterHandlerReturn` (windowed to a tight range).
       (b) When both stale and fresh are emitted to the spy, the filter drops the stale and returns the fresh → handleReset returns `{ok:true, mapReceived:true}`.
       (c) When ONLY the stale is emitted (no fresh within 3000ms window — simulate by spy returning null after timeout), handleReset returns `{ok:false, stage:"mapTimeout", epoch:7}` — correctly surfacing that reset fired but no post-reset map arrived.
     This test pins two contracts: `handleReset` MUST compute `minStampMs = Date.now()` right before the deserialize call AND MUST forward it to `waitForNextMap(timeoutMs, minStampMs)`. AND the `waitForNextMap` implementation in Plan 02 MUST apply the filter inside its message handler (not at a higher tier).

  6. **"clean unsubscribe on slamDeserializeEmpty-failure branch — no leaked subscription" (BLOCKER #1 regression gate)** — Handler architecture post-revision: `waitForNextMap` is NOT started until AFTER `bumpEpoch` resolves (so D-15 rejection has nothing to clean up — leak-free by construction). The drain contract therefore applies to the OTHER path: when the subscription has already started (step 6 of handleReset) and `slamDeserializeEmpty` rejects (step 7), the handler MUST `await waitPromise.catch(() => null)` before returning the HTTP 502 response, otherwise the rosbridge WS and topic handler stay alive for up to 3000ms AND Node logs an unhandled promise rejection if waitForNextMap's own WS errors in that window. Test mechanics: stub `bumpEpoch` → resolves `{epoch:7, resetAt:"2026-04-17T..."}` (must succeed so handler proceeds to the subscribe step). Stub `waitForNextMap` → returns a Promise you control (`let rejectWait; const waitPromise = new Promise((_, rj) => { rejectWait = rj; });`) and wraps it with a sentinel: `const observed = { drained: false }; waitPromise.catch(() => { observed.drained = true; });` — this sentinel only flips true when someone else also attaches a `.catch()` to drain the rejection path (Node's host promise tracker discharges the rejection once any handler is attached). Stub `slamDeserializeEmpty` → rejects with `new Error("deserialize fail")`. Call `handleReset()`. After it resolves, trigger `rejectWait(new Error("ws disconnected"))` to simulate waitForNextMap's own promise rejecting asynchronously (the realistic failure shape), then `await Promise.resolve()` twice (two microtasks) to let the drain `.catch()` run. Assertions: `response.ok === false`, `response.stage === "service"`, response HTTP status 502, response body.error contains "deserialize fail", AND `observed.drained === true` (proves the handler attached a `.catch` to waitPromise before returning). Rationale: this is the ONLY failure branch in the post-revision architecture where waitPromise outlives the handler; the test pins that the drain is NOT forgotten on that branch. Plan 02 step 7 explicitly enforces `await waitPromise.catch(() => null)` before the 502 return.

Use the test skeleton pattern from docker-adapter.test.mjs. Import target is `../lib/server/reset-handler.mjs` (Plan 02 creates this as a .mjs so node:test can import without TS compilation). route.ts stays a 2-line wrapper.

**Caveat on TS test import:** node:test as invoked by `node --test __tests__/` does NOT natively compile .ts files. Plan 02 enforces the architecture: reset logic lives in `.mjs` (testable), `route.ts` is a thin wrapper (not directly tested). The test file therefore imports `../lib/server/reset-handler.mjs`, never `../app/api/map/reset/route.ts`.
  </action>
  <behavior>
    - Happy path: stubbed bumpEpoch + deserialize + waitForNextMap(fresh-stamp) → {ok:true, epoch, mapReceived, elapsedMs} + HTTP 200
    - Service failure: stubbed deserialize rejects → {ok:false, stage:"service", error} + HTTP 502
    - Map timeout: stubbed waitForNextMap resolves null → {ok:false, stage:"mapTimeout", epoch} + HTTP 200 (epoch still bumped)
    - D-15 ordering: bumpEpoch rejects → service-stage response + deserialize NEVER invoked (tracked by a spy)
    - Stale-latch filter: waitForNextMap called with minStampMs; message with stamp<minStampMs is dropped; only message with stamp>=minStampMs satisfies mapReceived:true
    - Clean unsubscribe on slamDeserializeEmpty-failure branch (Plan 02 step 7): waitForNextMap promise is drained (catch attached) before handleReset returns the HTTP 502 service-stage response — the only failure path in the post-revision architecture where the subscription has already started
  </behavior>
  <verify>
    <automated>cd /Users/danny.smolinsky/jam-dev/MowerBot/web && npm run test -- __tests__/map-reset-route.test.mjs 2>&1 | grep -E "(not ok|ENOENT|Cannot find)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at web/__tests__/map-reset-route.test.mjs
    - 6 `test(` calls covering the behaviors above (happy, service-fail, mapTimeout, D-15, stale-latch, clean-unsubscribe)
    - Uses mock.module() (Node 22 native) OR a comparable fake-injection pattern
    - RED at Wave 0 (imports `../lib/server/reset-handler.mjs` which doesn't exist yet)
    - D-15 ordering test explicitly asserts slamDeserializeEmpty was NOT called when bumpEpoch rejects
    - Stale-latch test (#5) asserts minStampMs is forwarded to waitForNextMap AND messages with older stamps are rejected
    - Clean-unsubscribe test (#6) asserts waitForNextMap promise is drained on the slamDeserializeEmpty-failure path (Plan 02 step 7) — not the D-15 path, which is leak-free by construction in the post-revision architecture (subscription starts after bumpEpoch resolves)
  </acceptance_criteria>
  <done>Test file is RED against not-yet-shipped reset-handler.mjs; will go GREEN when Plan 02 lands the handler + route.ts wrapper.</done>
</task>

<task type="auto">
  <name>Task 5: Confirm Phase 6 regression gate still green + commit Wave 0 artifacts</name>
  <files>(no source edits; commit-only)</files>
  <read_first>
    - web/__tests__/server-upgrade.test.mjs (the regression gate — must still assert exactly 1 server.on("upgrade") listener)
  </read_first>
  <action>
1. Re-run the Phase 6 regression test to confirm it passes unchanged:
   `cd /Users/danny.smolinsky/jam-dev/MowerBot/web && npm run test -- __tests__/server-upgrade.test.mjs`
   Expected: 3 tests pass (one-handler, rosbridge-branch, logs-branch).

2. Run the full suite to capture the red/green snapshot:
   `cd /Users/danny.smolinsky/jam-dev/MowerBot/web && npm run test 2>&1 | tee /tmp/phase8-wave0-test-snapshot.txt`
   Expected:
     - server-upgrade.test.mjs: 3 pass
     - since-preset.test.mjs + demux.test.mjs + docker-adapter.test.mjs: all green (unchanged from Phase 6)
     - map-epoch.test.mjs: ALL RED (ENOENT / Cannot find module)
     - map-frame-pose.test.mjs: ALL RED
     - map-store-rehydrate.test.mjs: ALL RED
     - map-reset-route.test.mjs: ALL RED

3. Git commit the Wave 0 artifacts as two commits (fixture + scaffolds) for clean history. Use absolute paths in the add and commit via heredoc:
   `cd /Users/danny.smolinsky/jam-dev/MowerBot && git add config/empty.posegraph config/empty.data`
   `git commit -m "feat(phase-8): add empty.posegraph fixture for Option-B reset"`
   `git add web/__tests__/map-epoch.test.mjs web/__tests__/map-frame-pose.test.mjs web/__tests__/map-store-rehydrate.test.mjs web/__tests__/map-reset-route.test.mjs`
   `git commit -m "test(phase-8): RED scaffolds for map-epoch / mapFramePose / rehydrate / reset-route"`

4. Paste the last 30 lines of /tmp/phase8-wave0-test-snapshot.txt into the commit summary for future cross-reference during Wave 1 (so the developer can see which tests flipped from RED→GREEN after Plan 02).
  </action>
  <verify>
    <automated>cd /Users/danny.smolinsky/jam-dev/MowerBot/web && npm run test -- __tests__/server-upgrade.test.mjs 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - server-upgrade.test.mjs passes (3/3 tests green)
    - Full suite run captured at /tmp/phase8-wave0-test-snapshot.txt showing 4 new test files all RED
    - Two commits land in git with clean messages
    - `git log --oneline -3` shows the two Phase 8 Wave 0 commits
  </acceptance_criteria>
  <done>Phase 6 regression gate confirmed green; Wave 0 test scaffolds and empty.posegraph fixture committed; Plans 02/03/04 now have a fixed RED→GREEN contract to satisfy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer laptop → Pi (SSH) | Only Task 1 crosses; authenticated via password per MEMORY.md/ssh-pi-access.md. One-time fixture generation, no runtime path. |
| slam_toolbox container → host filesystem | `config/empty.*` written via docker cp (fallback) or bind-mount. Slam cannot choose path — filename is operator-supplied and stays under /config or /tmp. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | config/empty.posegraph (binary fixture committed to repo) | mitigate | File is small (<10 KB), binary diff-visible in git. Any malicious modification would break the Plan 02 deserialize round-trip — the integration test in Plan 02 Task X will catch a corrupted fixture before it ships. Also, `git show --stat` on the commit in Task 5 makes the file's provenance auditable. |
| T-08-02 | Information Disclosure | SSH session to mower Pi (Task 1) | accept | Trusted-LAN posture per STATE.md + CONTEXT.md. No secrets exchanged; serialize_map produces fixture data containing no PII (empty map has no scans, no positions — just an empty pose-graph header). |
| T-08-03 | Denial of Service | docker compose restart slam (Task 1 step 3) | accept | One-off operator action during phase planning, not runtime. Temporary interruption of mapping; recoverable with no persistent effect. |
| T-08-04 | Elevation of Privilege | Test mock.module() stubbing at test time | accept | Mocks only live inside the test process; cannot leak to production runtime. |
</threat_model>

<verification>
## Plan-level verification gates

1. **Fixture provenance:** `file config/empty.posegraph` reports binary, non-zero size. Plan 02 will do the functional end-to-end test when slam-reset.mjs calls deserialize with this fixture.

2. **Test suite RED baseline:**
   `cd web && npm run test 2>&1 | grep -c "not ok.*map-"` should equal (number of test cases across the 4 new files) — confirms ALL new tests are RED, none accidentally pass against stubs.

3. **Phase 6 regression gate:**
   `cd web && npm run test -- __tests__/server-upgrade.test.mjs` → 3/3 green. **This gate must stay green through every subsequent Phase 8 commit.** Make a note in the Plan 02/03/04 summaries to re-run it.

4. **No source edits outside __tests__/ and config/:**
   `git diff --name-only HEAD~2 HEAD` after Task 5 should show only: `config/empty.posegraph`, `config/empty.data`, `web/__tests__/map-*.test.mjs` (4 files). No changes to server.mjs, no changes under web/lib/**, no changes under web/app/**.
</verification>

<success_criteria>
- config/empty.posegraph + config/empty.data exist, committed, binary-valid (checked via deserialize smoke call on hardware in Task 1 step 9)
- web/__tests__/{map-epoch, map-frame-pose, map-store-rehydrate, map-reset-route}.test.mjs all exist, all RED (ENOENT on target imports)
- web/__tests__/server-upgrade.test.mjs still GREEN (3/3)
- Full `npm run test` output captured for Wave 1/2/3 to compare against (RED→GREEN progression is the Nyquist feedback signal)
- Two commits land in git with clear messages distinguishing the fixture from the test scaffolds
</success_criteria>

<output>
After completion, create `.planning/phases/08-lidar-map-anchor-persistence-honest-reset/08-01-SUMMARY.md` per the SUMMARY template. Include:
- How the empty posegraph was generated (Pi hostname, docker compose restart timing, serialize_map call output)
- The RED snapshot from `npm run test` showing exactly which tests fail with which error messages (this is the Wave 1 entry contract)
- Confirmation that server-upgrade.test.mjs still passes (with output pasted)
- The two commit hashes
</output>
