# slam_toolbox params — Phase 4 Plan 04-01
# Source: upstream humble mapper_params_online_async.yaml (verbatim) + project OVERRIDEs
# See .planning/phases/04-live-mapping-slam-toolbox/04-RESEARCH.md for per-key rationale
slam_toolbox:
  ros__parameters:
    # Plugin params
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ROS Parameters
    odom_frame: odom                  # explicit (upstream default) — pinned
    map_frame: map                    # explicit (upstream default) — pinned
    base_frame: base_link             # OVERRIDE: upstream is base_footprint; our TF tree has no base_footprint — RESEARCH Pitfall #2
    scan_topic: /scan                 # explicit — pinned
    use_map_saver: false              # OVERRIDE: no disk persistence in v0
    mode: mapping

    # if you'd like to immediately start continuing a map at a given pose
    # or at the dock, but they are mutually exclusive, if pose is given
    # will use pose
    # map_file_name: test_steve
    # map_start_pose: [0.0, 0.0, 0.0]
    # map_start_at_dock: true

    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.05    # OVERRIDE: 20 Hz map->odom (cheaper than upstream 0.02/50 Hz)
    map_update_interval: 2.0          # OVERRIDE: 0.5 Hz publish — satisfies SC#1
    resolution: 0.05
    min_laser_range: 0.05             # OVERRIDE: LD19 minimum
    max_laser_range: 12.0             # OVERRIDE: LD19 datasheet practical max
    minimum_time_interval: 0.5
    transform_timeout: 1.0            # OVERRIDE: slack for IMU-only EKF jitter — RESEARCH Pitfall #1
    tf_buffer_duration: 30.
    stack_size_to_use: 40000000       # program needs a larger stack size to serialize large maps
    enable_interactive_mode: true

    # General Parameters
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.1      # OVERRIDE: mower moves slow; upstream 0.5 m rarely fires — RESEARCH Pitfall #3
    minimum_travel_heading: 0.1       # OVERRIDE: ~5.7° — same reason as minimum_travel_distance
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: false            # OVERRIDE: Pi 4 CPU budget + v0 simplicity — RESEARCH Pitfall #6
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Correlation Parameters - Correlation Parameters
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Correlation Parameters - Loop Closure Parameters
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan Matcher Parameters
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0

    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
    min_pass_through: 2
    occupancy_threshold: 0.1
